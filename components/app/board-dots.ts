// The dotted board behind the tutor's handwriting, shared by the settings
// page ("What your tutor reads") and onboarding (the tutor's notepad). The
// dots are the ink at 11% so they read as paper, not as a grid.
export const BOARD_DOTS = {
  backgroundImage: "radial-gradient(rgba(18,18,21,0.11) 1px, transparent 1.2px)",
  backgroundSize: "18px 18px",
  backgroundPosition: "9px 9px",
} as const;
