# tutor-app — notes for agents and future sessions

## This is NOT the Next.js you know
This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

## What this is
A live voice tutor for students in grades 5–12 with a shared whiteboard. The student talks; the tutor talks back and writes on a tldraw canvas as it teaches. Brand working name: Chalk. `PRODUCT.md` is the design brief.

## Stack
- Next.js 16 App Router, React 19, TypeScript, Tailwind v4 (CSS-first, tokens in `app/globals.css`).
- Voice: **OpenAI GPT-Live-1** over WebRTC (`gpt-live-1`). Teaching brain: a Responses model via GPT-Live "Responses delegation" (`gpt-5.6-terra` by default).
- Voice fallback: **Gemini Live** (`gemini-3.1-flash-live-preview`, one model talks and draws) behind the same client surface; picked per session, see "Voice provider switch".
- Board: tldraw v5 + KaTeX (`components/TldrawCore.tsx`, `lib/semantic-board.ts`).
- Auth: next-auth v5 (JWT). DB: Neon Postgres via Drizzle (`lib/db/schema.ts`). Route protection in `proxy.ts`.

## Architecture (branch `gpt-live`, Sept 2026)
- `app/api/live-session/route.ts` — POST with the browser's SDP offer. Loads the student's profile and memory notes, composes both prompts, registers the whiteboard tools with the backend, calls `client.live.create`, returns the SDP answer plus the greeting/resume lines. The OpenAI key never leaves the server.
- `lib/live-tutor.ts` — browser client: mic track + `oai-events` data channel, transcript assembly, backend tool loop, speaking meter (RMS on the remote track), reconnect-with-history on drop.
- `lib/live-events.ts` — pure reducers (`TranscriptAssembler`, `BackendTurnTracker`), unit-tested.
- `lib/tutor-prompts.ts` — the prompts, built from named sections. Voice model: persona, speaking style, backchannel, interruption, delegation, boundaries. Backend: output contract, then the teaching sections (see "How the tutor teaches"), the whiteboard section, the student profile and memory, files/safety/reconnect, and eight teaching-moment examples. Gemini gets the conversation sections minus delegation, plus the same teaching sections.
- `lib/whiteboard-tools.ts` — the tool declarations (single source of truth) and `WHITEBOARD_FUNCTION_TOOLS` (Responses format). `lib/whiteboard-tool-dispatch.ts` validates arguments, calls the board handle, and returns a result that describes the picture ("Drew 1 circle cut into 2 equal parts, 1 shaded"); the session page appends a `[Board: …]` summary so the backend knows what the student sees.
- `lib/board-diagrams.ts` — pure, unit-tested math and parsers behind the picture tools (fractions, tick steps and fraction labels, intervals/jumps syntax, figure vertices and label placement, sketch strokes). `components/TldrawCore.tsx` turns the parsed inputs into tldraw shapes.

- `app/api/profile/notes/route.ts` — durable tutor memory (`user_profiles.tutor_notes`), written by the `remember_about_student` tool and loaded into the backend prompt at session start.
- Voice previews are gone (Sept 14): the old `app/api/voice-preview` route played OpenAI TTS, which failed once the account ran out of credits and never matched the Gemini voice a session actually uses.
- `app/session/[id]/page.tsx` — session orchestration and persistence (transcript events, board snapshots, heartbeat, pause/resume).

## How the tutor teaches (Sept 15 2026)
- The prompt follows the tutoring research in the "How Chalk Should Teach" review (expert tutors, Bridge, Tutor CoPilot, worked examples and fading, feedback research). Teaching comes before the board in the prompt: "What you are for", "How every turn works" (classify the move, decide what they need, end with something for the student to do), "Reading the student's answer" (correct/unsure/slip/wrong idea/partial/guess, and never call a wrong answer right), "How much help" (levels H0 wait → H5 worked example then fade; novices start high, students who are getting it start low), "Feedback and praise" (praise the move, flag mistakes indirectly), "Adapting up and down" (feelings first, confusion is not an emergency, raise the challenge when they cruise), "The session" (warm-up from notes, reflection after a solve, closing check and "Today's rule"), "When they want the answer" (a worked parallel problem as the fair fast path).
- Removed on purpose: "EVERY reply includes at least one board action", "start as high on the hint ladder as you can", immediate cross-outs of wrong answers, and repeated example openers ("Totally fair"). Onboarding slider preferences now appear only as "Stated preferences (a soft default; trust what you see them do over this)", and only when set.
- Gemini used to lose the backchannel, interruption, and boundaries rules (the persona was sliced at "# Backchannel policy"). Fixed; a unit test guards it.
- **Tutor tools and policy (Sept 15 2026; merged Sept 16).** The app-owned tool lives in `lib/tutor-tools.ts`, declared apart from the whiteboard tools and added next to them in `gemini-live.ts` (setup), `app/api/live-session/route.ts` (Responses tools) and `scripts/eval-tools.ts`. `check_answer(problem, student_answer, skill, help_level?, kind?)` runs `lib/answer-check.ts`, a deterministic checker on `math-expression.ts`: arithmetic, fractions, percent and "of", mixed numbers, simple LaTeX, implicit multiplication, one letter renamed to x, linear and quadratic equations (it knows the solutions, so "3" for x² = 9 is `partial`), and expression equivalence by sampling. It returns correct / partial / incorrect / cannot_check, never guesses on ambiguous input ("3 4", two letters, inequalities), and may give the right value "for you only". It also records the attempt in `lib/tutor-policy.ts` (`attemptFromVerdict`: correct, partial, `unchecked` for cannot_check, which counts neither way, and for a wrong answer the `kind` the tutor saw, slip, misconception or guess, else `incorrect`). A right answer's result asks for `circle_item` with keep=true, and for an equation, a check. `record_attempt` was merged in on Sept 16 2026: no recorded session had ever called it.
- The policy (pure, unit-tested, one instance per live client; replaces the old `tutor-state.ts`) keeps attempts, misses in a row, quick-right streaks, and signals read from whole student utterances (frustrated, confused, idk, bored, unsure; each counts for two utterances). It builds one line: `[Tutor state: skill "adding fractions": 0 of 2 right · 2 misses in a row (last: stuck) · suggested help H3 (2 misses in a row) · down: sounds frustrated (…) · 9 min in. Not from the student; never read it aloud.]`. Tutor tools always answer with it; board tool results carry it only when it changed (`takeStateUpdate`). Nothing is injected as an extra model turn any more (the old Gemini downshift made the tutor speak again after its turn). Gemini collects input-transcript fragments and reads the utterance once the tutor answers or calls a tool; GPT-Live reads flushed student utterances and typed text. Attempts are session-only for now.
- The prompt says: call check_answer before calling an answer right or wrong, with the skill and the kind of mistake when you can tell, and follow the suggested help level unless what you see says otherwise; the examples show it. `lib/tutor-tools.test.ts` fails if a prompt example names a tool that is not declared.
- **The teaching loop around the tools (Sept 16 2026).** Recorded sessions judged 20+ answers by eye, did arithmetic out loud that never reached the board, saved nothing to memory, opened "Problem 8" when the student said "part c", and left cancelled drawings on the board. What changed:
  - **Nudges** (`lib/tutor-policy.ts`, each carried once by the next board result through `boardResultExtras`, in both clients and the evals): an answer-shaped student line nobody checked ("[Unchecked answer: … call check_answer]"), arithmetic said in a turn that drew nothing ("[Said, not written: …]", from `spokenMath` in `noteTutorTurn`), memory due after a first misconception or when the student says goodbye with nothing saved, and every six results or three minutes the uploaded files ("[Files: File 1 "sheet.pdf" (3 pages). … call look_at_worksheet first …]").
  - "I don't know" while a skill is going is recorded as a `stuck` attempt with no tool call (`isNonAnswer` plus the idk signal). `cancelAttempt(callId)` takes back what a cancelled `check_answer` recorded.
  - **`look_at_worksheet(file?, page?)`** (`lib/session-tools.ts`, a third tool family next to board and tutor tools): the page shows the chosen page again as a picture, spaced at least a second from board frames, then answers "Here is File 1 "…", page 2 of 3". `resolveWorksheet` picks the file by label or name words and remembers the last page.
  - **`look_at_board`** now sends its picture before it answers (it used to answer first, so the tutor described an older picture), waits at most 2.5 s for the writing to finish, and says "The board is empty." when it is.
  - **Tool calls in Gemini Live** run on their own queue (audio keeps flowing), may be async, and are capped at 3 s: every tool blocks 3.1 Flash Live until it answers. `toolCallCancellation` reaches `onToolCancelled`: the page calls `undoCall(callId)` (below) and records `tool.cancelled`; a response for a cancelled call is dropped.
  - **Undo** (`TldrawCore.undoCall`): every shape a call draws carries `meta.callId` (through `withDirectMeta`), items and queued jobs carry it too. Undo drops the call's queued writing and marks, deletes its shapes and items, reverts line marks it set (`mathChangesRef`), and for `start_new_problem` puts back the board it cleared (`clearedBoardsRef`, the last three). `/dev/board?demo=cancel` runs all four cases (calls are `c0`, `c1`, … by position; a `__undo` step takes one back).
  - New client surface (`TutorClient`): `sendImageFrame(dataUrl, caption?)` and `sendStudentEvent(text)` (a user turn, for Explore in Phase 9); `LiveTutorCallbacks.onToolCall` gets the call id and may return a promise.
- **The prompt, rewritten around the board (Sept 17 2026).** What the recorded sessions got wrong is now written as rules in `lib/tutor-prompts.ts`, and `tutor-prompts.test.ts` checks each one is there:
  - **Teaching.** After a guess, "I don't know" or "what?", the tutor never answers its own question. "What?" usually means the student lost the question, so it asks again in fewer words. "I don't know" means make it smaller: two choices, a picture, or the first step done. The closing never ends on "I don't know".
  - **Board rules.** `boardSection(desmos)` now opens with four rules. Rule 4: what you work out goes on the board in the same reply (the problem as printed, the formula with its numbers, each step you say, the check), their right answer is ringed with `circle_item` keep=true, labels carry units, and nothing that gives the answer away is written before they say it.
  - **Board moves.** For a worksheet, call `look_at_worksheet` first. Attempts are the student's exact words. Every mark is the one sky pen. Leave `place` out. Callouts hold prompts; praise and chat are said, not written.
  - **Routing.** With Desmos, anything on a coordinate plane goes to `draw_desmos`: expressions, points, polygons, sliders like `m=2:-5..5`, and an empty grid (only the bounds). Data goes to `draw_data_plot`, categories to `draw_bar_chart`, values to `add_table`. `draw_figure` draws numeric lengths to scale, and grid=true lets the student count squares.
  - **Without Desmos.** When `desmosConfigured()` is false (a production build with no key), the prompt keeps the old `add_function_graph` / `plot_points` / dot-plot lines. Both builders take `PromptOptions { desmos?: boolean }`, defaulting to `desmosConfigured()`.
  - **Examples.** In `examplesSection(desmos)`, Example 5 is a right answer: `check_answer`, the attempt, a kept ring, and a two-line `draw_desmos` (`draw_equation_step` without Desmos). Example 9 is "what?": point at the item and ask a smaller question.
  - **`add_callout`.** It keeps a question that follows a praise sentence (`withoutPraise` in `lib/board-content-rules.ts`): "Exactly. What is 11 minus 3?" is written as "What is 11 minus 3?".
  - **Sizes.** The backend prompt ceiling is 19,600, up from 19,200. With the test profile the backend prompt measures 19,594 after Explore's clause (19,574 before it; an earlier note said 19,543, which was stale), six under the ceiling, so the next rule has to pay for itself. The Gemini prompt has its first ceiling, 20,800, and measures 20,692. Without a profile: backend 19,300 (18,951 without Desmos), Gemini 20,352. The whiteboard tool schema went from 41 tools at 44,085 characters to 37 at 38,815, under the 39,000 test.
  - **Eval.** `board-eval --set sessions` (flash-lite, 6 scenarios × 6 turns), Sept 16 baseline → Sept 17:

    | Measure | Sept 16 | Sept 17 |
    |---|---|---|
    | answers checked | 2/5 | 4/5 |
    | right answers ringed | 0/2 | 1/1 |
    | pointing | 11% | 22% |
    | Desmos share of pictures | 58% | 91% |
    | board use | 83% | 81% |
    | picture turns | 31% | 28% |
    | phantom claims | 0 | 0 |

    The run's one error was a praise-led callout, fixed by `withoutPraise`. One typo ("ij") was still written as an attempt; typos are left alone for now.

    `board-eval --model gemini-3.1-flash-lite --set desmos --scenario slope-graph,system,inequality,pattern` (the four scenarios the Sept 16 baseline finished before the free tier's daily quota ran out), Sept 16 → Sept 17:

    | Measure | Sept 16 | Sept 17 |
    |---|---|---|
    | answers checked | 3/10 | 5/10 |
    | right answers ringed | 0/3 | 2/3 |
    | pointing | 15% | 30% |
    | Desmos share of pictures | 5/8 | 5/6 |
    | board use | 75% | 80% |
    | spoken math left unwritten | 1 | 0 |
    | attempts written (none bad) | 1 | 6 |
    | picture turns | 35% | 30% |

    Picture turns dipped in both sets (a turn in twenty) while text-only turns rose: rule 4 puts steps and attempts on the board, and those count as text. Watch it in real recordings before adding a rule for it. `tutor-eval` was not rerun: the free tier allows about one persona a day on the flash models.
- Still not built (planned in the review): a learner model that persists skills and session summaries across sessions, a student pen on the board, Gemini VAD/thinking tuning, onboarding changes.
- **Teaching eval.** `npx tsx scripts/tutor-eval.ts [--persona misconception,anxious] [--turns 6] [--tutor gemini-3.5-flash] [--student gemini-3.5-flash-lite] [--judge gemini-3.8-flash] [--out file.json] [--verbose] [--prompt path]`. `--prompt` loads another copy of `tutor-prompts.ts` (for example `git show HEAD:lib/tutor-prompts.ts > /tmp/old/tutor-prompts.ts`) so two prompt versions can be compared on the same students. Seven simulated students (misconception, careless, answer-seeker, anxious, bored-advanced, novice, idk) talk to the real Gemini prompt, tools, and dispatcher on a fake board; a judge grades every tutor turn: mistakes caught, wrong answers confirmed, answer leaked, targeted remediation, ends with a student action, generic praise, help-level fit, 2+ questions, words per turn, board use, and whether the persona's success condition was met. The key is on the free tier: gemini-3.1-pro has no quota (limit 0), and flash models allow 20 requests per day per model, while one persona uses about 15 tutor calls. So on the free tier the eval runs roughly one persona a day; an old-vs-new comparison needs billing on the key. The script fails fast on both quota errors instead of waiting. Simulated students are too agreeable (they drop wrong ideas easily), so treat the numbers as a comparison between prompt versions, not a measure of real learning.

## The board (Sept 2026 rebuild)
- Picture tools, all rendered from real tldraw shapes: `draw_fraction` (pie or bar, improper fractions get extra wholes, typeset "= 3/4" beside the model), `add_number_line` (step, fraction tick labels, dots, shaded intervals with open/closed ends and rays, hop arrows), `draw_figure` (triangle, right triangle, square, rectangle, circle with side/vertex/angle/radius/diameter labels), `draw_angle`, `draw_array` (with dashed group splits), `add_area_model`, `draw_balance` (equation as a scale), `draw_bar_chart`, `draw_sketch` (polylines in a 0–100 box, the escape hatch). `add_function_graph` and `plot_points` draw real graphs (see "Graphs: Desmos"); nothing is a PNG any more.
- Style rules in `TldrawCore.tsx`: `INK` (black) for structure, `PENCIL` (the custom `pencil` colour, #5b5b66, 6.9:1 on white where tldraw's grey was 2.4:1) for the student's attempts and captions, and a marker palette (`MARKERS`: blue, violet, green, orange, red) for the tutor's drawings. `takePens(n)` hands each new diagram, and each series inside one, the next pen, so two fractions, five bars or three forces never share a colour; the counter resets on `start_new_problem`. **Every tutor mark is sky** (Sept 16 2026, Mateo's call, so the board matches the app's `--lp-sky`): highlights, rings, underlines, boxes and the pointer use the custom `sky` colour (#3d9cff); strikes use `sky-deep` (#1d7ee6), because #3d9cff is 2.9:1 on white and a line through black text has to read; typeset lines use the same two (`MATH_CORRECT_HEX` / `MATH_WRONG_HEX`). The three colours are new names added through `TLThemeDefaultColors` in `components/board/board-theme.ts` and passed to `<Tldraw themes>`, so the diagram pens look exactly as before. Headings are sans; notes and student work are tldraw's handwriting face; diagram labels are sans. Box heights come from `measureText` (tldraw's own text measurer), never from character counts. Diagrams use solid strokes; only `draw_sketch` keeps the hand-drawn wobble.
- Typeset math is a real tldraw shape: `components/board/MathShape.tsx` (`type: "math"`, registered through `TLGlobalShapePropsMap`) renders KaTeX in an `HTMLContainer`, measures itself off-screen at creation (`measureMath`), and carries `annotation` (16 px board sans in #5b5b66 since Sept 16 2026, identical in `measureMath`, the view and `toSvg`; it was 13 px grey), `crossOut`, `highlight`, `reveal` (the writing animation) and `role` (`"label"` for the "= 3/4" captions beside fraction models, which `highlight_step` / `cross_out_step` skip). `toSvg` exports the line as plain text (`lib/latex-plain.ts`) with its cross-out and ring, so board pictures need no compositing. That export is what the tutor and the admin replay see, so a bug there blinds the tutor to its own board while the student sees correct KaTeX: until Sept 15 `\pm` matched the front of `\pmod` ("±od6"), `\equiv`/`\mid`/`\nmid` were stripped as unknown commands, and `\_` printed its backslash. `latex-plain.test.ts` now covers the cases real sessions produced. `createMath` in TldrawCore creates them; `mathOrderRef` keeps creation order for "newest line" lookups. Old snapshots with `eqItems` overlay records are turned into shapes on load. `.chalk-math .katex-display { margin: 0 }` keeps the box tight.
- Camera: `focusOn` treats "visible but zoomed out below 0.8" as not visible, so new content always comes back to a readable zoom (this is why the first drawing looked tiny when the student had zoomed out). Since the Sept 14 layout work it frames the whole board page when the page fits on screen at that zoom, so the board holds still while the tutor writes, and otherwise (phones) it frames the item's row from the page's left edge, so a row never loses its start off screen. Since Sept 16 2026 it plans one camera with `planCamera` (`lib/board-camera.ts`, pure, unit-tested) and sets it once, animated over 320 ms. It used to call an animated `zoomToBounds` and then clamp to 0.8; the clamp read the zoom before the animation had moved, and its own `setCamera` cancelled the first move, so the camera zoomed in place instead of travelling to the item, and a 390px phone sat at 0.55. A phone now lands at 0.8 in one move. A rect that fits at that zoom is centred, its padding shrinking first (Sept 17: a 420px graph kept its left padding and lost 16px on the right), and a lone item wider than the screen at 0.8 (a 560px number line) is framed whole with tight margins, down to 0.55. It is a no-op while a tool call is still drawing; `endItem` focuses once the drawing is placed.
- Backend prompt (`lib/tutor-prompts.ts`) has a board-first rule: one diagnostic question at most, then draw the picture in the same reply as the idea; never describe a picture in words or fake one with brackets; one to three board actions per reply. Examples B, B2, B3 show fractions, a sketch and the balance.
- **Board use, fixed Sept 15 2026** after measuring every recorded session (55 tool calls). What was wrong: two sessions had ZERO tool calls while the tutor said "Focus on the table on the board" and "On the board, I've drawn a triangle" (students answered about a blank board); 24 of 41 shipped tools had never been called once, `draw_figure` and `draw_sketch` among them; text containers were 47% of all calls against 20% real pictures. The tools were registered the whole time, so this was the model taking the cheap path, not a regression. Evidence and numbers: `project_board_use.md` in memory.
  - The board section now opens with **three rules that override the rest**: (1) the first reply on a new topic calls `start_new_problem` and draws it in the same reply, and a topic with no picture tool is still drawn (`draw_sketch`, `draw_figure`, `add_table`, `add_number_line`); (2) never mention board content that was not drawn; (3) words on the board are labels, not explanations. The ceiling in `tutor-prompts.test.ts` moved 19000 -> 19200 to pay for them (268 chars net after trimming the lines they replaced). Raise it again only for a rule that earns it.
  - **The caps are enforced in the dispatcher, not just described**: `add_text_note` 160 chars, `add_worked_example_box` 140 chars over at most 3 lines (`NOTE_MAX` / `BOX_BODY_MAX` / `BOX_BODY_LINES` in `lib/whiteboard-tool-dispatch.ts`). Over the cap the call is refused with the instruction ("say the explanation out loud, or draw the idea instead"), because the model ignores tool descriptions but reacts to tool errors.
  - `draw_sketch` is now the declared **fallback** for any topic without a picture tool, instead of a tool whose own description told the model to prefer others.
  - `sendInitialGreeting`'s "Do not start teaching until the task is identified" now carries the counterpart "the moment they name it, your first reply about it puts it on the board", and the resume instruction no longer says "do not use whiteboard tools until a concrete task is confirmed" (it kept the anti-hallucination half).
  - `scripts/board-eval.ts` scores two new things: **picture rate** (a real diagram, not another box of words) and **phantom board claims** (the tutor saying "on the board" when nothing was drawn that turn). Run it before and after any prompt change here.
- **Board content (Sept 16 2026).** A recorded session wrote 23 texts and 13 had a defect. `lib/board-content-rules.ts` (pure, unit-tested) now checks every text before it is written, and a refusal says what to do instead:
  - `boardLines`: a literal `\n`, or " | " between words, is a new line (a note printed "Point 1: (2, 3) | Point 2: (6, 11)" with its pipe). `splitSlots` keeps empty pipe slots, so `annotations: "| | take the square root"` lands on the third line; `splitSteps` splits a block on " | " so an absolute value survives.
  - `add_student_attempt` refuses non-answers in every session language (`isNonAnswer`: "i dont know", "what?", "no sé", "不知道"…), text about the student ("you said…"), and anything over 200 characters. A bare "no" is allowed: it can answer a yes-or-no question.
  - `add_callout` is one small sky tag sized to its words (at most 360 px and 120 characters; an old `style` is ignored). Praise ("Perfect slope calculation!") and chat without math ("Let's wrap up… Ready?") are refused (`calloutProblem`).
  - Duplicates: each call's content is fingerprinted into `ItemToken.content` and `BoardItem.content`. The same text since the problem started, the same line as the newest equation, or the same picture as one of the last six is not written again; the tool points at the item and says "Already on the board as b2".
  - `start_new_problem` answers "…(item b1). Next, write the problem itself exactly as given, then work under it." A follow-up sentence goes after the item id through the dispatcher's `NEXT` separator.
  - Right triangles take `side_labels` as 'bottom leg | upright leg | hypotenuse' (`figureSideLabels`; "6 | 8 | x" used to put 8 on the hypotenuse). Three numbers that only fit Pythagoras another way are moved, and the result says "legs 6 and 8, hypotenuse x".
  - Sketch labels (`placeSketchLabels`): a label that says rise or height sits beside the most upright stroke near it, one that says run, width or base under the flattest, and every label is pushed off strokes and off other labels.
  - The vector number line prints the value of every dot between ticks, in the dot's colour, and stacks labels that would collide into lanes (`labelLanes`). Table and area-model cells read like math ("x^2" shows as x²).
  - `latexToPlain`, which is what the tutor reads back from its own board, writes real superscripts and subscripts (x², x¹⁰, a₁, e⁻ˣ; x^(q) where Unicode has no form), ∛ for `\sqrt[3]`, and keeps function names (`\log_2 8` used to read "₂ 8"). `normalizeLatex` braces multi-digit powers (`x^10` set as x¹0) and leaves two-letter products in math (`y = mx + b` set "mx" upright). `normalizeForMatch` ignores `^`, `_` and braces, so `highlight(text: "x^2")` finds x².
  - Cut from the declarations: `add_two_column_comparison`, `add_vector_diagram` and `add_process_map` (gone), and `highlight_step`, `clear_whiteboard` and `add_coordinate_axes` (still replayed by the dispatcher for old recordings, scripts and the landing). The `column` parameter is gone from every tool; the dispatcher still reads it. The schema went from 43,513 to 35,748 characters (41 tools to 35), and `whiteboard-tools.test.ts` keeps it under 39,000. Dead handle methods went too: `drawShape`, `addStickyNote`, `addTwoColumnComparison`, `addVectorDiagram`, `addProcessMap`, `captureScreenshot`, `applyBoardActions`, and the no-op `ensureColumnRoom`.
  - `/dev/board?demo=content` replays all of it.
- **Free visual QA:** `npm run dev` then open `/dev/board?demo=fractions|algebra|geometry|data|all&step=300&count=N`. It mounts the whiteboard and replays scripted tool calls with no OpenAI session (`app/dev/board/demos.ts`). The route is dev-only (404 in production, public in `proxy.ts` only in development). Screenshot it before touching a renderer.

## Math only (Sept 14 2026)
Chalk tutors math and nothing else (arithmetic, fractions, decimals, percent, ratios, negatives, algebra, geometry, graphs, basic statistics). The prompts say so in the persona, a "# Math only" section, and the Gemini contract; the landing FAQ, onboarding examples, and debug panel match. The board section carries a topic playbook: which picture tool for which topic. Picture tools added for the math focus, all in `components/TldrawCore.tsx` with pure helpers in `lib/board-diagrams.ts`:
- `draw_tape_diagram` (bar model: rows of boxes, `*` shades a box, `= total`, a bracket for the grand total) for ratios, parts and totals, a fraction of an amount.
- `draw_grid` (rows × columns, first N shaded) for percent, decimals, fraction of a set, area as counting squares.
- `write_vertical` (stacked +, −, × with carries and partial products, right-aligned mono) and `draw_long_division` (bracket, quotient, step lines; leading spaces place digits).
- `draw_transversal` (two parallels and a transversal, angles 1–8 clockwise from upper left, `mark_angles` arcs).
- `draw_figure` grew parallelogram, trapezoid, rhombus, pentagon, hexagon, and 3D rectangular_prism / cube / cylinder (`side_labels` = dimensions), plus `height_label` (dashed altitude with a right-angle mark). Side labels are positional: "12 | | 6" skips the right side.
- `add_function_graph` grew `mark_points`, `slope_run` (a rise/run triangle), and `second_expression` (two curves, crossing point marked: systems of equations); `draw_array` grew `shaded`; `draw_angle` grew `adjacent_degrees` (angles on a line / around a point); `add_number_line` grew `label_style` (tenths read as decimals by default) and `second_min`/`second_max` (a double number line for percent of an amount); `plot_points` grew `connect` (join into a polygon).
- `draw_fraction` grew `common_denominator` (1/2 and 1/3 recut into sixths); `draw_grid` grew `shade_rows`/`shade_columns` (rows tinted, columns tinted, overlap in full colour: multiplying fractions); repeated number-line points stack into a dot plot. Note, callout, setup, and table results quote their content so the tutor can point at or erase them by what they say.
- **Real things** (Sept 14 2026): `draw_icons` puts rows of everyday objects on the board (apples, cookies, coins, cars, animals, balloons…) with `group_size` gaps, `crossed` red X's, and a `second_icon` row, for counting, sharing, taking away, ratios, and everyday analogies. Icons are Fluent Emoji Flat (Microsoft, MIT) from `@iconify-json/fluent-emoji-flat` (devDependency): `scripts/build-icons.ts` curates the list (380 names since Sept 15 2026) into `lib/board-icons.generated.ts` (SVG bodies, client only, 467 KB: it rides in the session bundle, so weigh that before widening again) and `lib/board-icon-names.generated.ts` (names, used by `resolveIconName` and the eval, no longer by the declaration). `components/board/IconShape.tsx` is the `icon` shape (inline SVG, exports itself, reveal pops in). Re-run the script after editing the list. The MIT notice lives in the generated file header.
- **Icons, fixed Sept 15 2026.** A 34-picture session asked for 55 apples three times, got 40 each time, and was told it had drawn 40: `draw_icons` silently clamped the count and built its result from the clamped number. The clamp is gone. The ceiling is 120 and a bigger number is refused with a reason (draw one group and label it, or use `draw_tape_diagram`), so the tutor can recover in the same turn instead of lying to the student. Three more changes came with it: `group_size` now packs **whole groups per row** (two groups of 5 sharing a row read as one group of 10, so a row holds a whole number of groups and never splits one); `arrange` picks the picture (`rows` wraps, `array` with `columns` is the multiplication picture, `ten_frame` draws framed tens, `ring` seats things in a circle, `groups` boxes each group); and the declaration no longer ships a closed `enum` of every name — 380 names would cost tokens in every session's tool schema, so unknown names are resolved loosely and refused with near matches (`suggestIcons` in `lib/board-icons.ts`). `/dev/board?demo=icons2` replays all of it, 55 apples included.
- LaTeX from the model goes through `lib/latex-normalize.ts` in the dispatcher (unicode operators and superscripts, vulgar fractions, degrees, bare `%`, words wrapped in `\text{}` with their spaces, units upright, lone `a/b` as fractions, `\\` line breaks split into separate lines except inside environments). `renderMathHtml` tries the LaTeX as written, then normalised, then as plain text; KaTeX error markup never reaches the board. Equation rows are pitched by measured height (`max(EQ_H, h + 6)`).
- Icon names resolve loosely (`lib/board-icons.ts`: plurals, phrases, aliases like kids/money/ice cream). Erasing an item leaves its space free for the next drawing; nothing slides up (the column reflow was removed with the Sept 14 layout work). Board items are saved in `WhiteboardSnapshot.items` and rebuilt from `meta.itemId` for older snapshots, so resumed sessions keep their ids.
- `/dev/board?demo=math`, `?demo=icons`, `?demo=icons2` (layout edge cases), and `?demo=eqs` (LaTeX torture: unicode, tall lines, bad input, marks, erase, cases) replay all of it. `/dev/board?script=<URL-encoded JSON [{name,args}]>` replays arbitrary calls; the eval logs' `↳ tool({args})` lines can be lifted straight into it to see the board a model actually built.
- `cross_out_step` (and `highlight_step`, no longer offered but still replayed) fall back from equation lines to any item by label (sky ring, underline or box; deeper sky strike), because models mark the student's attempt with them. Student attempts are labelled by their text. Item bounds read the full text of a line still being written (`revealTextRef`).
- Eval on the 12-topic math set, before → after the math work (gemini-3.1-flash-lite): board use 97% → 96%, pointing 28% → 35%, sketch-or-text fallbacks 12 → 9 turns; the after run's tool errors were all target-matching cases fixed since (token matching, newest-line default, caption-preserving labels). Equation-line labels are plain text (`lib/latex-plain.ts`) so `point_at "5/6"` resolves. The eval has a `--set math` scenario set (12 topics).

## The tutor's hands (Sept 14 2026)
- **Board items.** Every tool call is one item with a short id (`b1`, `b2`, …). `dispatchWhiteboardTool` wraps each call in `board.beginItem/endItem`; the id rides in the result ("… (item b7)") and `getBoardSummary()` returns the item list (`formatBoardItems` in `lib/board-items.ts`) that the page appends as `[Board: …]`. Items resolve by id, label words, kind, or `last` (`resolveItemTarget`, unit-tested). Item bounds measure rendered text and typeset math, not their wide layout boxes.
- **Pointer and rings.** The tutor is a tldraw collaborator: an `instance_presence` record (`userId: "tutor"`, sky) drawn by `components/board/TutorPenOverlay.ts`, which replaces two of tldraw's overlays: the collaborator cursor (a pen glyph and a name tag in #1d72dc, so its white text passes 4.5:1) and the collaborator scribble (`TutorScribbleOverlayUtil`; tldraw painted the laser ring at 10% alpha, invisible on a real board, and ours draws the presence colour at 70% and fades). `point_at` glides it onto an item with a small tap; `circle_item` draws that laser ring (presence `scribbles`, fades after ~3 s) or, with `keep=true`, a sky ring that stays. `erase_items` / `erase_older` delete items (shapes + typeset lines). The cursor hides after ~6 s idle.
- **Highlighter** (Sept 14 2026). `highlight(target, text?)` runs a real tldraw `highlight` stroke (drawn behind the ink) over the exact words in an item, e.g. `text="2x"` in an equation line or a tick label on a number line, or over the whole item. A drawing gets a slim marker ring instead: a highlighter ring smothered the drawing, and tldraw's highlight `scale` cannot thin it because its SVG export divides positions by the scale. Short words need care: tldraw's `getStrokePoints` drops points within a third of the stroke size of either end and skips the first three points until the stroke is a full stroke-size long, so a short, sparse swipe collapsed into a dot at its left end. Short swipes use 1px point spacing and a run of at least two thirds of the stroke size plus 4px (`swipePoints`). There is one colour, sky (Sept 16 2026: the four meaning colours are gone and an old `color` argument is ignored), so the tutor says what a mark means. A target taller than it is wide (a stacked fraction) gets a vertical swipe (`highlightSwipeFor`). The dispatcher checks the words synchronously (`normalizeForMatch` / `matchVariants` in `lib/board-items.ts`, which also match a typeset fraction whose DOM has no slash and puts the denominator first), so the tool result says honestly when it fell back to the whole item. The stroke is placed by measuring the words in the shape's DOM (`[data-shape-id]`, text-node ranges only so KaTeX's tall layout boxes never count, `.katex-mathml` skipped, math annotations skipped) and converting with `editor.screenToPage`; it queues behind the writing, joins the item (erased with it), and exports with board pictures. `highlight_step` is no longer offered (Sept 16 2026); `circle_item` with keep=true rings a whole line. `/dev/board?demo=highlight` replays it.
- **Written, not pasted.** `revealItem` hides everything a tool call created and a queue writes it in reading order with the cursor leading (`lib/board-reveal.ts` plans order and durations): text types out, strokes and lines grow point by point (original points kept in `strokePointsRef`), boxes and arrows fade, typeset math wipes in. The pace is an unhurried hand: about 20 characters a second, a 4.5 s cap per tool call, a pen glide and a short pause before each big piece (the Sept 14 speed was 1.8 s and read as rushed). `catchUpPace` speeds a call up only when tool calls are queued behind it. Pointing and rings queue behind the writing, and the queue always starts on a `setTimeout(0)` after the tool call returns (see the next bullet). `onWriting` keeps the dock badge on "Writing on the board" while the queue runs. Reduced motion skips the animation.
- **Marks are never items** (Sept 16 2026). A highlight ring once sat in empty space: with the queue idle, the highlight ran inside its own tool call, so `endItem` registered the mark as a new item and nudged it into free space. Now every tool has a role (`toolRole` in `lib/board-items.ts`: draw, mark, erase, look, memory), `endItem` registers and places only `draw` calls, and marks always run from the queue. When a mark runs it finds its target again, measures it without earlier marks (`meta.mark`) or the decorations beside it (`meta.decor`, e.g. the "you" tag on an attempt), draws, and joins the item, so erasing the item erases its marks. In development `checkMarkLanded` warns when a mark lands away from its target. Kept rings are superellipses that grow until they enclose the corners at any aspect ratio (`ringPoints`, tested from 1:1 to 20:1). `/dev/board?demo=rings` replays every kind of mark; run it at `&step=6000` too (the idle-queue case) and under reduced motion.
- **The tutor sees the board.** `exportImage()` waits for the queue, then renders the board to a JPEG (every shape exports itself; math shapes export as plain text with cross-outs and highlights). Exports are white like the board on screen: the board theme sets `background: "#ffffff"` (Sept 17 2026; tldraw's own #f9fafb framed every white Desmos picture in a faint box). The session page sends one to Gemini as a `realtimeInput.video` frame about 1 s after each tool call, and immediately for `look_at_board`. GPT-Live has no frame path yet (`TutorClient.sendBoardFrame` is optional).
- **Prompt.** "Every turn happens on the board": board moves by situation (new topic, explaining, asking, student answers, confused, referring, finished), pointing while talking, erasing when done, self-check from the picture. Examples A–G all include board moves. Same text feeds the GPT-Live backend and the single Gemini prompt.
- **Offline eval.** `npx tsx scripts/board-eval.ts [--model gemini-3.1-flash-lite] [--set core|math|icons|sessions|desmos|all] [--scenario fractions,system] [--turns 6] [--out file.json] [--verbose]` replays scripted student lines through a Gemini text model with the real prompt, tools and dispatcher against a fake board and prints board-use, pointing, erase and error rates per scenario. The key is on the free tier (about 5 requests a minute), so a full run takes ~15 min; the script waits out 429s. No audio, a few cents. **The default model is `gemini-3.5-flash`**, whose free tier runs out after a scenario or two; the Sept 16–17 before and after runs all used `--model gemini-3.1-flash-lite`, and runs only compare on the same model. flash-lite's daily quota covers about three sets.
- **Dev handles.** In development, `window.__chalkEditor` (tldraw Editor) and `window.__chalkBoard` (the `WhiteboardHandle`) are exposed on the session and `/dev/board` pages. `/dev/board?demo=marks` replays point/ring/erase; `?demo=rings` every kind of mark.

## Session recording and admin replay (Sept 15 2026)
Mateo wants every beta session recorded in detail, so the two of us can find where the tutor goes wrong. It is on for every user; only his account can look.
- **The recorder.** `lib/session-recorder.ts` runs on the session page.
  - **What it records.** Every debug event from the live client and the page arrives as `live.debug`, in every session (until Sept 16 `recordDebug` returned early outside `?debug=1`, so normal sessions recorded none of it; only the debug panel is gated now, and the repetitive `tool_call_received` / `board_frame_sent` notices are skipped). That covers each tool call with its arguments, result and duration (including `check_answer` and `record_attempt`), interruptions, turn ends, "[Tutor state]" lines, reconnects, go-aways and errors. It also records `tool.call` for board tools, `tutor.speaking` on and off, `tutor.activity`, `session.started` (provider, speech rate, viewport, user agent), `settings.speed` and `board.frame`. These sit alongside the existing `transcript.entry`, `whiteboard.snapshot` and `session.*` events.
  - **How events are sent.** Events are batched to `POST /api/sessions/[id]/events` (body `{ events }`) every 1.5 s or 40 events, and handed to `sendBeacon` when the page hides. Failures are dropped after three tries and never disturb the session.
  - **Order (Sept 16).** Replayed tutor lines used to come back word-shuffled ("this can be kind of problem … tricky"). Three causes, all fixed:
    - every transcript fragment was its own fire-and-forget POST;
    - the events route read the highest `seq` and then inserted, so concurrent requests shared numbers (1,039 duplicate groups in 23 of 48 sessions);
    - the Gemini socket read Blobs asynchronously.
  - **How order is kept now.**
    - Transcripts, the intake line, snapshots and `session.ended` all go through the recorder.
    - The recorder stamps a client sequence (`cseq`) on every event and sends batches strictly one at a time.
    - `appendSessionEvents` (`lib/db/session-events.ts`) numbers a batch in one statement: the UPDATE of `tutor_sessions.event_seq` locks the row until its INSERT is done.
    - `compareEvents` (time, then `cseq`, then `seq`) orders the replay, the export and the review page.
    - The Gemini socket uses `binaryType = "arraybuffer"` and a message chain.
  - **Spacing.** Transcript fragments keep the spacing the model sent. Once a fragment shows a boundary space the stream is marked `spaced`, and `joinTranscript` (`lib/live-events.ts`) concatenates exactly ("tri"+"cky"); older trimmed recordings get spaces added back.
  - **Snapshots.** `recordLarge` sends board snapshots whole (the normal path clamps long strings), each in its own request, and skips one identical to the last.
  - **Board pictures.** The recorder keeps the exact JPEG sent to Gemini after each drawing. Providers without automatic pictures get one 1.2 s after a board tool call instead, and there is a last one at session end (capped at 2.5 s). They go to `POST /api/sessions/[id]/frames` and are stored once per session by SHA-1 in `session_frames` (base64 in Postgres, about 20–30 KB each).
- **What the tutor "thinks" is not available.** Gemini Live (`gemini-3.1-flash-live-preview`) accepted `thinkingConfig.includeThoughts` but returned no thought parts on a test turn (Sept 15 2026). Tool calls with their results and the tutor-state lines are the closest record of why it acted.
- **Database.** `session_frames` is created by `lib/db/sql/2026-09-15-session-frames.sql`, which is additive and idempotent. There is no migrations folder, so run that file on any new database. When it was added, the Neon database held 14 MB.
- **Event numbering migration (Sept 16), in two steps.**
  - **Step A:** `lib/db/sql/2026-09-16-event-seq.sql` adds `tutor_sessions.event_seq` and `session_events.client_seq` and fills the counter. It must run before the code that uses them. It was applied to the Neon `main` branch on Sept 16 after a test branch passed: 25 concurrent writers, 100 contiguous seqs, also under the unique index.
  - **Step B:** `lib/db/sql/2026-09-16-event-seq-unique.sql` renumbers old collisions (keeping their order) and adds the unique index on `(session_id, seq)`. **Applied to the Neon `main` branch on Sept 22 2026** with Mateo's OK, after production had run the step A code since Sept 18: 1,018 duplicate groups in 22 old sessions renumbered in one transaction, all 14,330 events kept, `session_events_session_seq_uidx` in place and the old non-unique index dropped.
  - Dev and production share the one Neon branch, so a local run already writes to the real database.
- **Admin pages.** `/admin` lists every session with student, length, talk, tool calls, issue chips and picture count. `/admin/sessions/[id]` is the replay.
  - **Access.** `lib/admin-emails.ts` allows msosaalbrecht@gmail.com, plus the QA account in development only. `getAdminSession()` in `lib/admin.ts` guards every page and route, and anyone else gets a 404. The sidebar shows "Recorded sessions" to admins.
  - **The replay.** It shows stats, the board picture at the playhead, and a timeline with lanes for student speech, tutor voice, actions, board pictures and issues. Playback runs at 1×, 2×, 4× or 8×, with Space and arrow keys. A searchable, filterable log sits beside it, and clicking a row seeks.
  - **Exports.** `/api/admin/sessions/[id]/export?format=md|json` gives a Markdown timeline for close reading, or everything as JSON.
- **Analysis.** `lib/session-recording.ts` is pure and unit-tested. It:
  - merges transcription fragments into utterances;
  - counts tool calls without double counting;
  - finds failed tools, interruptions, reconnects, errors, silences of 20 s or more, and approximate reply times;
  - builds the log and the Markdown export.

  Sessions from before the recorder show their transcript only.
- **Free QA without a live tutor.** Sign in with `/api/dev/qa-login?to=/admin`. Create a session with `POST /api/sessions`, send a synthetic `{ events }` batch and a few real board pictures (`window.__chalkBoard.exportImage(896)` on `/dev/board`) through the real routes. Open the replay, then `DELETE /api/sessions/[id]`, which cascades to events and pictures. Background tabs do not run animation frames, so bring the replay tab to the front before testing playback.
- **Privacy.** Beta testers may be minors. Students and parents should know sessions are recorded, and the privacy notice should say so.

## Graphs: Desmos (Sept 15 2026; hardened and widened Sept 17)
Mateo found the old graphs "not good" (a fixed 300 × 220 box, numbers only at the four ends, 121 straight segments, a steep curve flattening the rest, and plain writing like "3x − 2" or "|x|" drawing "Could not read that expression" while the tool told the tutor it had graphed it). He compared Desmos, JSXGraph and Mafs side by side on the board and chose Desmos. The comparison page and the other two libraries were removed.
- **What draws.** `add_function_graph`, `plot_points` and `add_coordinate_axes` (undeclared since Sept 16 2026, still replayed) create a `graph` shape (`components/board/GraphShape.tsx`, 420 × 315 plus a 14px credit strip; three fit across a laptop board page, 480 × 360 fitted two). Its picture is an SVG made by one hidden Desmos calculator (`components/board/desmos-renderer.ts`, `asyncScreenshot` with `format: "svg"`, about 8–25 ms a graph once Desmos has loaded; the script is 4.3 MB and loads with the first graph, never on pages without one). The shape keeps the graph spec (JSON) and the SVG: crisp at any zoom, wiped in by the writing animation like typeset math. Board pictures for Gemini wait for graphs still rendering (up to 8 s), and `toSvg` renders on demand and exports the "desmos" credit with the picture.
- **Spec version 2 (Sept 17 2026).** `lib/desmos-spec.ts` (pure, unit-tested) is the spec every renderer draws from: `v: 2`, `kind`, `size`, `bounds`, the full Desmos `settings`, the expressions and tables, `markers`, and optional `sliders`, `source` and `studentState` (for Explore). It lives inside the shape's `spec` JSON because the shape has no migrations; `parseGraphSpec` reads it and upgrades a version 1 spec (size from the shape, numbered axes, rise and run turned into markers, a `source` rebuilt from its `curve1…` expressions). `source` is what the tutor asked for (`function`, `points` or `axes` with their arguments), so a graph can be redrawn without Desmos. `markers` are the words on a graph: `kind: "point"` (a dot; the dab lands on it) or `kind: "label"` with an orientation (rise and run; the dab lands on the words, `markerBox`). Before this, a highlight on "rise 4" found no marker and fell through to the only text in the shape's DOM, the credit, so it highlighted "desmos"; a graph now never measures DOM text.
- **Axis numbers and grid.** Every spec names its steps (`axisSteps`): the smallest 1, 2 or 5 × 10ⁿ step that leaves room for its widest number (about 8px a digit on x, 28px rows on y), one step for both axes when the units are square, and minor lines at least 15px apart (`minorSubdivisions`; Desmos's own choice drew them 8px apart once the steps were set). The usual −5..5 view is numbered every unit with half-unit minor lines. `autoYRange` keeps the x-axis 10% above the bottom so Desmos has room for its numbers (at 8% it pinned them, greyed, to the edge).
- **The renderer (hardened Sept 17 2026).** Every render sends every setting (a graph used to inherit the previous one's), prefixes the expression ids with a fresh `g<n>_` so no analysis from an earlier graph is read, gives up on a screenshot after 6 s and rebuilds the hidden calculator, and waits up to 400 ms for `expressionAnalysis` when none of its ids is there yet. Load status is `idle | loading | ready | failed | unavailable` (`desmosStatus`, `onDesmosStatus`); a failed load is final for the page, so later graphs and exports never wait another 20 s. `preloadDesmos()` fetches the script when the browser is idle; the session page calls it while the session connects and records `live.debug` kind `board`, `desmos_status`, on every change. The landing never preloads. A render that fails for another reason is tried once more.
- **Saved boards leave the pictures out.** `getSnapshot` blanks each graph's SVG (a board of 8 graphs saved 27 KB instead of about 230 KB) and `loadSnapshot` redraws them once the items are back: 8 graphs in 155 ms with Desmos, as vectors without it (shown at once, not written in). The in-memory copy an undone `start_new_problem` restores keeps its pictures.
- **`lib/desmos-graph.ts`** (pure, unit-tested) builds the spec. `toDesmosLatex` rewrites plain math Desmos rejects (checked against Desmos: `sqrt(x)`, `abs(x)`, `|x|`, `sin(x)`, `x²`, `e^(-x)`, `x^-2`, `pi`, the Unicode minus) into LaTeX. `graphLatexProblem` uses Compute Engine to refuse structurally broken input up front (an unclosed brace), so the dispatcher returns an error the tutor can fix in the same turn; Desmos's own per-expression errors arrive after rendering and show in `[Board: …]` as "[could not draw: …]". `graphFunction` samples curves with Compute Engine 0.128, whose `compile()` is a free function returning `{ success, run }` (older versions had `expr.compile()`). `autoYRange` trims the far 4% tails so an asymptote cannot flatten a curve; `fitBounds` squares the units only when the ranges are already within 1.5× of square (so the x range the tutor asked for survives), and a circle always gets square units with its y range derived from x. Crossings of two curves are found numerically and labelled; the slope triangle is two dashed restricted lines with "run"/"rise" labels on points of size 0.01 (a point with `pointOpacity: 0` also hides its label in Desmos's SVG); joined points are an `\operatorname{polygon}`. Labelled points keep math coordinates, so `highlight(target, text: "vertex")` dabs the point itself.
- **The tool.** `add_function_graph` accepts LaTeX or plain math and gained `y_min`, `y_max` and `extra_expressions` (shaded inequalities, circles, vertical lines, restricted pieces, up to 6, separated by ";").
- **Licence and key.** Commercial use of the Desmos API needs a Desmos partnership (partnerships@desmos.com) and its key in `NEXT_PUBLIC_DESMOS_API_KEY`. Development falls back to the public demo key from Desmos's own docs, which is for local use only. A production build without a key, or a session where Desmos fails to load, draws vector graphs instead (next bullet), so **until the partnership key is in Vercel, production shows vector graphs**.
- **Without Desmos.** `drawVectorGraph` in TldrawCore draws a graph's `source` into the same 420 × 315 box, labels included (the plot sits 28–34px inside it), so the caption and layout do not move. A graph created before a failed load is swapped in place (`swapGraphToVector`): the vector shapes take the graph's meta (item, call id), replace it in its item, and are written in again; undoing the call erases them. Curves are read by Compute Engine first (LaTeX and plain math, so `\frac{1}{x}` no longer says "Could not read"), cut at the view's edge and at asymptotes; `y_min`/`y_max` are honoured. `vectorExtra` (pure, in `desmos-graph.ts`) reads each extra expression into what vectors can draw: `y` as a function of `x` (also anything linear in `y`, like `2x+3y<6`, and a piece restricted in `x`), `x = c` (a segment when restricted in `y`), and circles (read numerically, so expanded forms work), with the shaded side of an inequality. Boundaries are dashed for strict inequalities; a shaded side is a draw shape filled in the pen with `dash: "none"` (tldraw 5 draws no outline then) at opacity 0.22, under the axes, so two sides overlap darker where both hold. Anything else is left out. Images made with Desmos tools are CC-BY-SA with attribution, hence the small "desmos" credit under each graph.
- **Pictures on Desmos (Sept 17 2026).** Mateo asked for Desmos "for many different stuff". Pure builders in `lib/` turn a tool's drawing into a spec, each with tests: `desmos-primitives.ts` (polylines as point lists with `lines: true`, polygons, dots, pixel-true arrowheads, arcs, hops, ticks, labels, `WORD` = 1.25 label size), `desmos-number-line.ts`, `desmos-bar-chart.ts` (and `chartAxes`), `desmos-figure.ts`, `desmos-data.ts` and `desmos-free.ts`. Their words are our own labels, at 1.25 times Desmos's size so they read like the board's 18px text, and every word is a marker the tutor can highlight.
  - **Number lines** (`add_number_line`): our own line, arrowheads and ticks on a picture with no grid or axes, one unit up being one pixel. Every dot's value is written in its colour, fraction ticks are stacked fractions, crowded tick labels thin out by round steps (never every third), labels that would collide take the next lane, overlapping hops arch at different heights, rays end in coloured arrowheads, a dot plot is one colour, a double number line keeps room for its unit word. `stackBase`, `reserveAbove` and `unitLabel` serve the data plots.
  - **Bar charts** (`draw_bar_chart`) and **histograms** draw their own axes, numbers, unit word and gridlines (Desmos's axes run past the plot). The vector chart (production without the switch) drew no bar for a negative value until Sept 17; `barScale` (in `board-diagrams.ts`) now reaches below zero, a negative value's number sits over the zero line, and names wider than their slot take two rows.
  - **Figures** (`draw_figure`, flat ones; solids stay vector) are drawn **to scale** when the labels give lengths: a right triangle from any two sides, a triangle from three sides or base and height, a rectangle, a square, a parallelogram, a trapezoid from its bases and height or legs, a regular pentagon or hexagon, a circle from its radius or diameter; otherwise the vector proportions, and the result says "not to scale". Numbers that cannot make the shape (6, 8 and 11 as a right triangle) come back in the result as "Careful: …" whichever renderer drew it. `grid: true` puts it on a unit grid (area by counting squares; Desmos only). When the lengths fix every angle (three sides, or a right triangle from two lengths), numeric angle labels are checked against the drawing (`angleMismatch`, within 1.5°), because a to-scale picture makes a wrong label look right: sides 7, 5, 6 with 44° at B answer "Careful: with these sides the angle at B is 57°, not 44°" (the `figures` demo had exactly that until Sept 17).
  - **`draw_data_plot`** (new tool): a dot plot and a box plot (both standing on the number line builder, so they number their own axis; box plot quartiles the grade-6 way), a histogram (bins at x = 0, 1, … like bar slots, with our own edge numbers), and a scatter plot with a linear or exponential fit computed here, so the result can state "y = 2.02x − 0.02 (r = 1)". Without Desmos a dot plot becomes a vector number line, a histogram a vector bar chart, a scatter a vector graph with its points; a box plot is refused with a suggestion.
  - **`draw_desmos`** (new tool): whatever the tutor writes for Desmos, checked item by item (`classifyDesmosLatex`: relations, bare expressions in x, points and point lists, polygons, parametric curves with a `t=0..2\pi` domain, assignments, `y~mx+b` regressions over a two-column table, histogram/dotplot/boxplot), never forced into "y = …". A letter with no value becomes a slider at 1 and the result says so; `sliders` sets values and ranges; `settings` reads 'no grid', 'square', 'x step=2', 'x label=…', 'arrows', 'degrees', 'polar', 'log y'. The view is Desmos's own square view unless an x range was given or the curves would be off it (then y fits them), or there is data (then it fits the data); circles and parametric curves count as data. Refused with a fix-it message: broken LaTeX, unknown settings, a regression without a table, an item with nothing to draw, more than 8 items.
  - **Which renderer.** `lib/desmos-config.ts`: `NEXT_PUBLIC_DESMOS_TOOLS` ("all", "none", or tool names) picks the pictures drawn on Desmos; unset, **every build draws all three on Desmos**: Mateo compared them with the vector versions on Sept 17 2026 and chose Desmos ("desmos looks good"); set it to "none" to go back. The comparison is a private artifact, "Desmos Picture Review" (https://claude.ai/code/artifact/41ea7f0b-71a0-40a9-ae1b-dd826a2493e9, Sept 17): 15 pairs and 4 Desmos-only pictures, each the board export of one call drawn alone with `desmostools=none` and `all`, with the recommendation to set `all` once the partnership key is in. A picture uses Desmos only once Desmos has loaded (`desmosStatus() === "ready"`, so a number line never waits on the 4.3 MB script and never has to be swapped after a failed load); graphs still start on Desmos while it loads. `draw_desmos` and `draw_data_plot` are declared only where Desmos can load (`desmosConfigured()`), and the dispatcher refuses `draw_desmos` when the board says it cannot use Desmos. A picture whose drawing fails is redrawn by its vector tool and moved into the Desmos picture's place (`drawVectorPictureAt`); a box plot, with no vector version, stays a placeholder and the summary says "[could not draw: …]".
  - **Handle methods:** `desmosFor(tool, figure)` (the dispatcher asks before saying "to scale") and `drawGraph(build, {pens, label, column})` (the dispatcher checks a build first, then the board builds again with real pens).
- **Free visual QA:** `/dev/board?demo=graphs` (every graph tool, plain math converted, a slope triangle, an inequality with a circle and a vertical line, a triangle from points, a system of inequalities, a disc and a line piece, empty axes, highlights on a labelled point and on "rise 4"), `?demo=numberlines`, `?demo=bars`, `?demo=figures`, `?demo=plots`, `?demo=desmos` (sliders, a system, a table fit, a reflection, a circle with an ellipse, automatic sliders, and two refusals). In development `&nodesmos=1` behaves as if there were no key, `&desmosfail=1` fails the load 1.5 s in (the first graphs swap to vectors in place), `&desmosbreak=1` fails every drawing after a good load (pictures swap to their vector tools), and `&desmostools=none` draws number lines, bar charts and figures the vector way for side-by-side comparison. The demo page waits for Desmos to load before it replays, as a session does.
- **Desmos facts (verified Sept 16 on `/dev/desmos-probe`, API v1.12, screenshot path).** The probe page renders each case to SVG and prints pass/fail; `window.__probe` holds the summary.
  - **Text in the SVG is drawn as paths.** There are no `<text>` elements; every label also gets a `<title>`, so a test that searches the SVG for label text proves nothing. Judge labels by eye.
  - **Stable output.** The same graph twice gives the same SVG once ids are ignored.
  - **Settings that show in screenshots:** `showGrid`, `showXAxis`/`showYAxis`, `xAxisStep`/`yAxisStep`, `xAxisMinorSubdivisions`, `xAxisArrowMode`, `xAxisLabel`/`yAxisLabel`, `degreeMode`, `polarMode`, `xAxisScale`. `fontSize` and `projectorMode` through `updateSettings` changed nothing (they are constructor options). `setBlank` did reset `showGrid`, but send every setting on every render anyway.
  - **Axis numbers need explicit steps.** At the board's 420×315, Desmos's automatic step numbers the x axis only every 5 units and often leaves y unnumbered (at 320×240, no numbers at all). With `xAxisStep`/`yAxisStep` set (1 on a −5..5 × −4..4 graph, 2 or 5 on wider ranges) every major line is numbered. `xAxisNumbers: false` works.
  - **Labels.** Plain text, backtick math (`` `\frac{1}{2}` `` renders stacked), `labelSize: "2"`, lowercase `labelOrientation` (`"above"` is exactly `Desmos.LabelOrientations.ABOVE`) and `${m}` interpolation all work. A label survives `pointSize: 0.01` and `hidden: true`, but **`pointOpacity: 0` hides it**.
  - **Points and lines.** `pointStyle: "OPEN"` draws a hollow point. List broadcasting works (`x=[-4,-2,0]\left\{-0.3\le y\le0.3\right\}` is many ticks in one expression). Parametric curves take `parametricDomain: {min: "0", max: "1"}`. `\operatorname{polygon}` draws with `fillOpacity`. **`segment()` is not available** ("This calculator does not support the 'segment' function"), so segments are parametric.
  - **Data.** Tables (`type: "table"`, columns `x_1`/`y_1`, `points`/`lines`) plot. `y_1\sim mx_1+b` fits, and `HelperExpression({ latex: "m" }).numericValue` reads m back. `\operatorname{histogram}(list, width)`, `\operatorname{dotplot}(list)` and `\operatorname{boxplot}(list)` draw in the SVG; the bare names fail ("Too many variables").
  - **Sliders.** `m=1` with string `sliderBounds` works, and `y=mx` follows it.
  - **Errors.** `expressionAnalysis` is empty right after `setExpressions` but correct after the screenshot, and an id reused after an error reads fresh, so reading errors after the screenshot is enough.
  - **Hangs.** A calculator whose host is not in the document still screenshots (no hang seen); keep a timeout anyway.
  - **`secret: true`** keeps an expression out of a visible calculator's list, which the Explore panel can use to hide scaffolding.
  - **Console noise.** Desmos logs "Attempted to apply path command to node g" on screenshots; it is harmless.
  - **Axis numbers need a tall picture (Sept 17).** Desmos draws no axis numbers in a screenshot under about 260px tall (250 has none, 260 has them), whatever its width or the calculator's own size; number lines, dot and box plots, bar charts and histograms therefore write their own. Numbers pinned against the picture's edge turn grey, so a data view keeps 34px under its x-axis and 40px beside its y-axis.
  - **Text size.** The calculator's `fontSize` option changes nothing in screenshots; a label's `labelSize` does (decimals like "1.25" work), and stacked fractions scale with it.
  - **Label geometry (measured Sept 17 on the board's 420 × 315 picture).** Labels are about 6.8px a character and 17px tall ("rise 4" is 37 × 17), set about 6px from their point on the side `labelOrientation` names; with the default orientation Desmos picks a side itself (left, right, above or below, whichever is free). In the SVG each label is a `g.dcg-svg-label`; its `<title>` sits on the point's own group.

## Explore: live Desmos graphs (Sept 17 2026)
Mateo asked for the Desmos graphs to be interactive ("so the student can click around, change things"). A graph on the board stays a picture; Explore opens it as a live calculator.
- **The button.** Graphs of kind function, points, axes and free (`isExplorable` in `lib/desmos-explore.ts`) get an "Explore" pill in their top right corner once they are drawn and written in (`ExploreButtons` in TldrawCore). The pill is 28px inside a 44px target, icon only under 640px or on a small graph, and sits in the part of the graph that is on screen. Number lines, charts, figures and data plots get none: they are pieces laid out in pixels.
  - **Its own layer.** The pills live in a layer beside tldraw at z-index 5, not in tldraw's `InFrontOfTheCanvas`. tldraw's container is not isolated, so its in-front layer (z-index 250) painted the pills over the panel and would paint them over the dock; isolating the container instead would let the dock cover the tldraw watermark on phones.
  - **Who shows them.** Pages opt in with `onExplore` (the session page, `/dev/board?explore=1`); the landing's board has none.
  - **Pressed.** "Exploring" keeps ink text on the sky fill, with a deep-sky border: sky text on the fill measured 3.6:1.
- **The panel** (`components/session/GraphExplorer.tsx`), never modal: the student keeps talking and the board stays usable.
  - **Laptop.** A dialog over the graph it came from, placed by `lib/explore-panel.ts` (pure, tested): over the whole graph clear of the chips and the dock column; else reaching into that column, still above the dock; else beside the graph. It is 340px wider than the graph for Desmos's list (560 to 800px), grows out of the graph in 220ms (150ms out), and only fades under reduced motion.
  - **Phone** (under 768px, `useIsMobile`). A vaul bottom sheet (`modal={false}`, `handleOnly`) under the session chip; below 450px Desmos puts its list under the graph. vaul's Title and Description label the sheet with their own ids: passing any `id`, even `undefined`, breaks that.
  - **Header.** "Explore · caption" (the graph's caption, else its first line), one line of what to do (`exploreInvite`: "Drag the m and b sliders to see what they change."), Reset (disabled until something changed) and Close.
  - **Footer.** What the tutor has seen: "Your tutor will see what you change.", "…will see this when you pause.", "…has seen your changes.", or "Close this to keep your changes on the board." when the session cannot tell the tutor.
  - **States.** A faint grid while Desmos starts; "The live graph didn't open." with Try again; "Your tutor erased this graph." with Close.
  - **Keys.** Escape inside a Desmos math field leaves the field; Escape again closes, and focus goes back to the graph's button. Typing in Desmos leaves tldraw's tool alone.
- **The live calculator.** One at a time, destroyed on close; in development `window.__chalkExplore` is the live calculator.
  - **What it shows.** `exploreItems(spec)` hands it the graph's items with the scaffolding (slope triangles, labels) marked `secret`, and leaves out the slider caption. A graph of points becomes named draggable points `P_{i}` that its polygon follows, and draw_desmos's loose points can be dragged.
  - **Options.** The list, zoom buttons and keypad; no settings menu, links, folders, notes, images or actions; accent #1d7ee6.
  - **Bounds.** `fitExploreBounds` keeps the board picture's proportions, so a slope looks as steep as on the board, and `setDefaultState` makes Desmos's own reset return to the tutor's graph.
- **What the tutor hears** (`components/session/useGraphExplore.ts`, `lib/explore-report.ts`).
  - **What changed.** Every change is compared with where the graph started (`snapshotOf`, `diffExplore`, `describeExploreChanges`), for example "moved m from 1 to 3; moved C from (4, 3) to (5, 4); added y = -x". Undoing everything reports "put the graph back the way it started".
  - **When.** A report is a session event, and the tutor answers it, so it waits: 2.5s after the last change, at least 8s after the last report, and only while nobody is talking (the tutor silent for 1.5s and neither thinking nor writing, the student silent for 2s).
  - **How.** First a 640px JPEG of the live graph (`sendImageFrame`, at least a second after other pictures; GPT-Live gets it with a caption), then the event: `[Explore b2: the student moved m from 1 to 3. They are still exploring; a picture of their graph came with this. React to what they found in a sentence, and ask what they notice.]`.
  - **On close.** Whatever the tutor has not heard goes out at the next quiet moment, after a fresh board picture, and is dropped after 30s.
  - **Recording.** All of it is recorded as `live.debug` kind `explore` (opened, report, report_final, closed, report_dropped).
- **The board keeps the student's version** (`applyExploreSnapshot`, then `applyGraphSpec` on the handle).
  - **What carries over.** Slider values (and the caption), moved points (and their polygon), edited and removed lines, and the student's own lines in pencil grey as `student1`, `student2`… (numbered after lines from earlier visits). The old picture stays up until the new one is drawn.
  - **What the tutor reads.** `studentState.summary` keeps the changes across visits and `[Board: …]` shows them, for example `[the student changed it in Explore: moved m from 1 to 3; added y=-x]`. A graph with sliders or points shows `[Explore: sliders m, b]`.
  - **Erased while open.** If the tutor erases the graph during Explore, nothing is applied.
- **Prompt.** One clause for Desmos builds: sliders "show what a number changes: ask them to drag it in Explore". The `draw_desmos` description also says the student can drag sliders in Explore.
- **Both voice stacks** send reports (`sendStudentEvent` and `sendImageFrame` on `TutorClient`). Neither has been tried in a live session yet.
- **Free QA.** `/dev/board?demo=explore&explore=1` shows a slider graph, a triangle of points and a number line with no button. Reports go to the log, and `&tutor=off` shows a session that cannot tell the tutor.
- **Checked headless at 1440 and 390 (Sept 17).**

  | Measure | Laptop | Phone |
  |---|---|---|
  | Button target | 101×44 | 44×44 |
  | Panel | 760×547 | sheet 390×784 |
  | Live graph | 442×442 | 388×273, list below |

  Clicking a button neither pans nor selects. The desktop panel only fades under reduced motion.

## Learning evidence (Sept 18 2026)
Built by a Codex session on `codex/learning-evidence`, reviewed and merged here. It is the learner model the teaching review asked for: what a student can actually do, carried from one session to the next, and only ever from work the tutor checked.
- **Where the evidence comes from.** `check_answer` now also takes the `help_level` the student had (H0-H5; helped work is never H0), and a second tutor tool, `record_teaching_move`, records the move itself (skill, level, move type, optional misconception and remediation). Both answer through `TutorRuntime`.
- **`lib/tutor-runtime.ts`** is one pedagogical runtime shared by both voice transports and kept across reconnects and a provider swap: it owns the policy (the old per-client `createPolicy`), an assistance ledger (effective help is the highest level given on a problem), and an event sink. `GeminiLiveSession` and `LiveTutorSession` both take one and route tutor tools through `runtime.runTool`.
- **Projection.** `lib/skill-catalog.ts` is a closed catalog of 30 skills with aliases (a label the tutor invents resolves conservatively or not at all). `lib/learning-evidence.ts` turns immutable evidence into per-skill states (supported, review due, needs revisit, retained), requiring distinct problems and 24 hours apart before it calls anything retained. `lib/learning-overview.ts` builds the student's focus list and the brief.
- **What the tutor reads.** The brief goes into the next session's prompt through `app/api/live-token/route.ts`, capped at three priorities, and the prompt says the student's stated goal wins and this is "a fallible, time-sensitive guide", never a grade or an ability label.
- **Storage.** Four additive tables (`skills`, `teaching_moves`, `learning_attempts`, `learner_skill_states`) from `lib/db/sql/2026-09-17-learning-evidence.sql`, **applied to the Neon main branch on Sept 18 2026** (30 skills seeded). `lib/learning-client.ts` batches events like the session recorder (100 an request, beacon on hide) to `POST /api/sessions/[id]/learning`.
- **When the tables are missing** (a fresh database), `loadLearningOverview` catches and returns an empty brief, so sessions still start; the home card shows its own empty or error state with a Retry.
- **The home page** shows "What to work on next" from real states (`lib/home-focus.ts`), replacing the fabricated `FOCUS` placeholders. With no evidence it says so plainly.
- **The prompt changed with it.** The teaching sections were compressed to pay for the new rules (19,597 of the 19,600 ceiling; Gemini 20,695). The closing rule changed on purpose: the tutor offers an exit check and **respects a student who wants to leave**, where it used to be told never to end on "I don't know".
- **The evals answer tutor tools through the runtime** (`scripts/eval-tools.ts` takes a `runtime`), because `runTutorTool` alone does not know `record_teaching_move`: before that fix a sessions run logged 8 "Unknown whiteboard tool" errors that only existed in the harness.
- **What it costs the board.** `board-eval --set sessions` on flash-lite, 36 turns, the Sept 17 branch against this merge, twice (the first run alone looked much worse; most of it was noise):

  | Measure | Sept 17 | merge run 1 | merge run 2 |
  |---|---|---|---|
  | board use | 29/36 | 28/36 | 29/36 |
  | picture turns | 10 | 11 | 10 |
  | answers checked | 4/5 | 3/5 | 4/5 |
  | right answers ringed | 1/1 | 1/1 | 1/1 |
  | **turns with a pointing move** | **8 (22%)** | **5 (14%)** | **5 (14%)** |
  | **`add_student_attempt`** | **4** | **0** | **1** |
  | `record_teaching_move` | — | 12 (2 refused) | 13 |

  Everything the prompt hard-requires held. The two measures down in **both** runs are the two soft ones: pointing at what it just said, and writing the student's words. Those are the calls a turn drops first when something else takes the slot, and `record_teaching_move` is the most-called tool in both runs (one every three turns). `record_attempt` was cut in Sept 2026 for the same competition. Two offline runs on flash-lite is not a verdict; the direction is consistent, the size is about three of each per 36 turns. Watch it in real recordings before changing the prompt, and if it holds, make the bookkeeping optional rather than required before every hint.
- **It does not stall a live turn.** `TutorRuntime.runTool` answers `record_teaching_move` synchronously from memory; the row is batched to the server by `LearningRecorder`, off the voice path. So the cost is one extra round through the model's tool loop, not the 3 s block a slow tool would be. **Never run in a live session yet** as of Sept 18 2026.

## Session summaries (Sept 21 2026)
The first piece of the memory layer Mateo asked for: after a session ends, a cheap model reads the whole thing and writes the note the student sees. Until now a finished session had only its transcript, and the home page faked a recap out of the tutor's last sentence.
- **The model writes prose; we supply every number.** `sessionStats` (`lib/db/session-summary.ts`) counts from `learning_attempts`, the deterministic record, and the system prompt tells the model **never to state a number** because the counts are rendered beside its words. So a summary cannot claim "8 of 10 right" and be wrong. The same prompt bans praise that would fit any session, bans inventing, and says an empty "still shaky" list is a real answer.
- **What it returns** (`lib/session-summary.ts`, pure, tested): `headline`, `recap`, `wins` (≤3), `stuck` (≤2), `next`. `parseSummary` clamps lengths and drops blanks and duplicates rather than rejecting, but returns null without a headline or a recap, because there is nothing to show. The stored blob also carries the stats and the model name.
- **How it runs.** Ending a session sets `summary_state = 'pending'` in the PATCH that sets `status = 'ended'`, **server-side**, so a tab that closes still leaves the job. The browser then fires `POST /api/sessions/[id]/summary` to have it written now rather than on the next sweep. `GET /api/cron/summaries` (Vercel cron, **once a day** at 09:00 UTC, `vercel.json`: the Hobby plan allows no more, and Vercel refused the whole Sept 22 production deploy while it said every 10 minutes) picks up anything still waiting after a minute's grace, up to `MAX_TRIES` (3). Columns are in `lib/db/sql/2026-09-21-session-summary.sql`, **applied to the Neon main branch on Sept 21 2026**.
- **The model call** is one place, `lib/summary-model.ts`. Vercel's AI Gateway speaks the OpenAI API, so the SDK the GPT-Live path already uses reaches it with a different base URL: set `AI_GATEWAY_API_KEY` and models are namespaced (`openai/gpt-5.6-luna`); without it, it falls back to `OPENAI_API_KEY` and the plain model name. `SESSION_SUMMARY_MODEL` overrides either. `CRON_SECRET` authorises the sweeper and the calls it makes. **First run against the real model on Sept 21** over two recorded sessions (`scratchpad/try-summary.mts`, read-only): it worked in 3 to 6 s, stated no numbers, and knew one session ended mid-idea. Three things it got wrong are now rules with tests: Title Case headlines ("Solving Equations Through Balance and Perimeter"), a "still shaky" line about the teaching rather than the student, and report verbs ("recognized", "identified"). A fourth was ours: a recap cut mid-word by a blind slice; `clean` now backs up to a word and ends on an ellipsis. Sessions from before `learning_attempts` carry no answer counts, and a 75,000-character session is cut to 48,000 for the prompt.
- **The page** (`app/session/[id]/summary/SummaryClient.tsx`, behind a server `page.tsx` with `Suspense` for `useSearchParams`). Rebuilt three times on Sept 21–22; the shape Mateo kept came from OnePrep, which he pointed at for its titles and text ("a lot of tiny text", "find a new place for the conversation"). Top to bottom: the date, the headline and the recap, with a **"Read the conversation"** pill on the right; a **stat strip** (one bordered row split by 1px gaps: Time · Correct · With no hints · Problems, a 14px label over a 32–34px number; a cell with nothing to say is dropped rather than shown as zero, and an odd last cell spans both columns on a phone); **What clicked** and **Still shaky** as two equal-height cards, each with a 22px title and a one-line subtitle; **Try this next** as a pale-sky band with the sentence set large and the app's black `btn-gloss` "Start practice"; then **Your boards**, a section header (22px title, subtitle, arrows, "Open the board" pill) over the snap-scrolling strip, one card per problem. Nothing on the page is under 14px (measured). The conversation lives in a Base UI `Sheet` from the right, 460px on a laptop and full screen on a phone, loaded on first open (`useTranscript`, started by the click, not an effect). The sheet's own width rules are keyed on `data-side`, so overrides must be too (`data-[side=right]:w-full`); a plain `w-full` lost to its 75% and 384px cap. Rejected on Sept 22, do not bring back: five bolder directions at `/dev/summary` (a black verdict block, a chapter timeline, bento tiles, a split view, an editorial report; "dude too much"), and the conversation as a collapsed row at the bottom of the page. States, all shot at 1440 and 390: pending (skeleton stat cells and cards, "Writing your summary"), done, failed and unwritten (a card with Try again / Write one), a single board, and no board.
- **One board per problem** (`lib/session-boards.ts`, pure, tested). Each problem's finished board is the last picture recorded before the next `start_new_problem`; a problem opened and never drawn for is left out; a worksheet page sent as a picture (an `upload:` reason) is never a board; a recording with pictures but no markers shows its newest picture under the session's title. The 13-minute homework session has six ("Solving 3x + 7 = 25" … "Today's takeaway"), and the old page showed only the last of them. `GET /api/sessions/[id]/boards` derives them (ids only, never the pictures), `GET /api/sessions/[id]/frames?frame=<id>` serves one (scoped to the session, cached a day), `GET /api/sessions/[id]/transcript` returns whole turns through `mergeUtterances` (the session GET returns every event, snapshots included, far too much to read a conversation). The summary GET also returns the session's `title`, the heading when no note exists.
- **`?mock=done|short|pending|failed|noboard`** on the page, in development only (`mock.ts`), renders canned data and never touches the database, like `/session?mock=1`. The prose is real gpt-5.6-luna output; the counts, boards (the landing's board photographs) and conversation are invented.
- **The faked recap is gone.** `GET /api/sessions` returns the summary and `sessionHeadline` prefers it, falling back to the old guess for sessions from before this.
- **Not done:** memory itself. The plan is that the same post-session pass emits **operations** on a typed memory (add / revise / resolve / drop) rather than appending to `tutor_notes`, which would otherwise become a junk drawer. Two measured constraints for that work: the backend prompt with two notes and a learner brief already measures **19,708 against its 19,600 ceiling**, so memory needs a character budget and retrieval by today's skill rather than a dump; and `remember_about_student` has the same shape as `record_teaching_move`, which cost the board its pointing moves, so post-session writing should replace it rather than join it.

## Whiteboard layout (Sept 14 2026; sections and pages Sept 16)
Mateo found the tutor used the board "like a list" (everything stacked in one or two fixed columns) and kept it in the middle. The fixed-column layout is still how tools draw, but the result is moved:
- **Pages.** The board is a page the size of the visible board, captured on the first placement after `start_new_problem` (`pageFrame`, clamped to 900–1800 × 560–1100). Pages sit side by side (`PAGE_GAP`); a new one opens only when nothing fits (next bullets). `PAGE_INSET` keeps work clear of the session chip, the End button and the watermark; `DOCK_BLOCK` (384 × 282) keeps it out from under the voice dock. The frame, the current section's region and its row top are saved in `WhiteboardSnapshot.pageState` (older snapshots with only `sectionTop` still load).
- **Placement.** `endItem` measures what a tool call drew (`itemBounds`) and nudges it into free space found by `findSpot` in `lib/board-layout.ts` (pure, unit-tested): down the first column, then the next, never overlapping, reusing erased space. Defaults per tool: an equation goes under the previous equation of the same section, a picture goes beside the words just before it, graphs and axes go to the right half. The tutor can say where with a `place` argument on every drawing tool (`parsePlace`): "beside b3", "below b3", or any area the summary names (`AREA_NAMES`: the halves "left", "right", "top", "bottom"; "the left third", "the middle band" and the other thirds and bands; the nine cells "top left" … "centre" … "bottom right"; `areaFromWords` also reads "in the top-right corner", "center", "lower left"). A named place is looked for on the whole page, wholly inside the area if it fits, else centred in it, else nearest. The old `column` argument still works as left/right. Default work fills the current section's region, then spills onto the rest of the page, and only then opens a page. Headings place themselves and own their row: the problem heading the page's, a section heading its region's. Each item's placed rectangle is cached (`placedRectsRef`) because a reveal in progress changes live bounds.
- **Sections and pages (Sept 16 2026).** A recorded session (`session_1789536065273_3ncyif`) ran to four pages, about 4,800px wide, and at one point 8 of the 9 items the tutor referred to were on pages the student could not see. A section opened a page whenever the deepest item came near the bottom, even with two thirds of the width empty; every tool offered "new page"; and the free space the summary named ("bottom left", "the middle band") was not a valid `place`. Now:
  - `planSection` (pure, in `lib/board-layout.ts`) fills a page the way text fills one: a panel beside the current row of sections while there is width, else a band under all the work (a new row), else a panel under the right-hand work, else null (a new page). A region needs a clear 420 × 240 block (heading included) away from the dock. Section headings count as their words when planning, so the next section can sit beside them.
  - A section's first item that does not fit under its heading takes the heading with it (`rehomeSection` in the handle): to room on this page that holds both, else to a fresh page. A heading never sits apart from its work.
  - `place: "new page"` is honoured only at a section start on a mostly used page (`shouldOpenPage`: 6 of the 8 usable cells hold work, headings counted by their words, `usedCellsOn`), and the heading moves along; otherwise the result says "stayed on this page: there was room". "New page" is no longer in the tool description.
  - Tool results carry notes from `takeNotes()`: that, "b2 is on page 1, so the board turns there to show it" for a mark on another page, and `no room at "bottom left", so it is at the top middle`.
  - `focusOn` frames the page that holds the item (so pointing back turns the board to page 1), and on a phone an item in a right-hand panel is framed on its own instead of from the page's left edge.
  - Replay of that session's 68 calls at its 1454 × 818 board: before, 3 pages and 3,948px wide, with every listed item off-page after each section; after, 2 pages, both "new page" requests stayed, and the graph section moved to page 2 with its graph instead of leaving the graph under a callout.
- **What the tutor reads.** `[Board: …]` says where each item sits ("b3 fraction (top left)") and where the page is empty ("Free space: the right third"), from `regionName` / `freeSpace` over the same page area a named `place` uses, so every phrase it writes is a valid place (a test walks them all). Work on another page reads "(page 1)", and the summary ends "New work goes on board page 2; the student is looking at page 1. Items on another page are out of sight until you point at them."
- **Free visual QA:** `/dev/board?demo=layout` (default placement), `?demo=sections` (panels, rows, a refused new page, a named area, a new page when full, pointing back) and `?demo=highlight`. A whole recorded session is too long for `?script=`, so the page also exposes `window.__chalkDispatch(name, args)` in development; call it once per tool call and `await window.__chalkBoard.exportImage(64)` in between to wait for the writing.
- Handle methods added for this (`setPlacement`, `highlight`) are optional on `WhiteboardHandle`, so the eval scripts' `Partial` fake boards still compile.
- Board frames sent to Gemini are unchanged: a JPEG of the whole board after tool calls, no id overlays, default `mediaResolution`.

## App UI (branch `session-redesign`, Sept 2026)
- One flat surface, no panels on a gray backdrop. `components/app/AppShell.tsx` = shadcn `SidebarProvider` + `AppSidebar` (light, square, edge to edge, `collapsible="icon"`, Cmd+B) + `SidebarInset`. `components/app/TopBar.tsx` is the 52px header every page shares (sidebar trigger, page content, actions). Home, Settings, the new-session loader and every session state use it.
- The session screen is the board plus a **voice dock** (`components/session/VoiceDock.tsx`, bottom right, 380px): status badge (beUI `AnimatedBadge`), the tutor's voice as a dithered wave (`VoiceWave.tsx`, WebGL, driven by the remote-audio `AnalyserNode` that `LiveTutorSession` hands out through `onAudioAnalyser`), the mic button, and a keyboard button that morphs the row into a composer (Sona UI `MorphSurface`). Two pills above it: Files (`FilesPopover.tsx`, drop zone + chips; dropping anywhere on the board also works via `useFileDrop`) and Transcript (the dock grows to full height and shows `TranscriptPanel.tsx`). End session lives in the top bar behind a confirm popover. Captions are `CaptionBar.tsx`.
- Brand tokens (`--lp-*`) are defined on `:root` as well as `.lp`, so the app and the landing share one palette and one font stack (Hanken body, Schibsted display via `lp-display`, loaded once in `app/layout.tsx`). shadcn theme variables are tuned to them (ink primary, sky ring, light sidebar). `.lp-btn` is the one pressed CTA style; use it once per screen at most. **One black:** #121215 (`--ink`, `--lp-ink`, shadcn `--foreground`/`--primary`, `--sidebar`) for text, borders, dark surfaces and shadow tints. No #000, #0a0a0a or `bg-black` in UI code (Mateo, Sept 14). Exceptions: `#000` inside CSS masks (only the alpha matters) and tldraw's own color names on the board.
- **Home page directions** (Sept 15 2026, exploration): `/dev/home?v=1..21` (dev only, behind login; `&shot=1` hides the switcher, arrow keys switch) renders sixteen home page mockups inside the real `AppShell` on mock data (`app/dev/home/data.ts`, shared pieces in `parts.tsx`). Round one (1–8): the board, ask first, call your tutor, notebook, a note from your tutor, bento, night desk, today's plan. Round two (9–16), after Mateo asked for "more modern, less slop": table, sentence, sheet, doors, tiles, pages, dock, index; no greeting-and-date openers, no card grids or three-up columns, handwriting only as board content, concentric radii, one hairline ring instead of border plus shadow. Round three (17–21), after Mateo said he wanted bento, call your tutor and ask first "but better": console, studio, two doors, desk, stage. These five share `app/dev/home/pieces.tsx` (Composer, Suggestions, Panel, ContinueTile/Row, WeekPanel, MemoryPanel, TopicsPanel, EarlierList), which is where the composed-home craft rules live: outer radius 20 = inner 12 + 8 padding, one hairline ring that deepens on hover, 0.96 press, a 100ms staggered entrance, ink text on the shader (white fails on the light blue), placeholders at 4.6:1. Gotcha found here: a bare Tailwind `grid` sizes its one implicit column to max-content, so a long row overflowed at 390px; pin tracks with `grid-cols-[minmax(0,1fr)]`. `components/HomePage.tsx` is unchanged until Mateo picks one. Every direction needs real session titles (all sessions are still titled "Session") and a board picture per session.
- **Free design preview:** sign in, then open `/session?mock=1` (dev only). It creates a session and renders the live screen with a scripted transcript and a synthetic voice, never opening an OpenAI session. `/api/dev/qa-login?to=/` signs in as the QA user and lands on any path.
- The tldraw "made with" badge is moved to the bottom-left in `globals.css` so the dock never covers it (its license requires it to stay visible).
- The sidebar is near-black with off-white text: `--sidebar` #121215 (the brand ink) and `--sidebar-foreground` #ECECEF, dimmed rows at 60%. Pure #000 with pure white text glared (Mateo, Sept 14), and this still reads as black. Hard-coded whites in `AppSidebar` use the sidebar tokens. The header wordmark is 20px with a 29px mark (the brand 1.45 ratio); collapsed, the mark slides 6.5px left to centre in the 48px rail. Rows are plain (`.sb-btn` / `.sb-item` in `globals.css`): New session is a normal row, a little brighter than the rest at weight 500 (no border, fill, lift or blue: Mateo asked for it to be "just normal"), brightening on hover and pressing in to 0.98; the home page's Start a session button does the same through `.lp-btn-lift` (3px). The landing's `.lp-btn` still rests raised and presses in on hover. The sidebar toggle is a plain `PanelLeft` glyph, always full foreground (`.sb-toggle`), growing to 1.08 under the pointer. **No outlines, no gray fills and no lifts anywhere in the rail** (Mateo, Sept 14): the current page shows a sky icon, the open session a sky dot in the gutter, and hover only brightens the text; keyboard focus is the one ring, in sky.
- **Home** (`components/HomePage.tsx`, from Mateo's Figma, Sept 16 2026): the date, the greeting, then a grid. Left: a full-width "Start a new session" card whose lower 60% is a `FooterWave` gradient from a pale sky (#b2d6ff) through the bar blue to the today blue (#3a80ef), at speed 1.9 with swing 1.7 and `ramp` 0.38 (how far down the blue takes to deepen); tuned in a standalone render of the shader (`scratchpad/wave-harness.mjs`-style: about 22% of the band changes every 1.5 s, crests stay 25px clear of the top, and a bottom floor keeps troughs from showing white); it opens the intake at `/session`. Below it, "Past sessions": each with a thumbnail of its newest board picture, a title, a one-line recap, and the day plus minutes; "Show all" past six. Thumbnails come from `GET /api/sessions/[id]/frames` (owner only), and the list endpoint marks `hasPicture`, so rows without a picture show the dotted chalk placeholder instead of requesting a 404. Right: one tall card spanning both rows, split by a hairline: "This week" (practice time, active days, seven bars with tooltips on hover and focus, today in the deeper blue) and "Things you have to work on" (bigger type; each item a skill, a plain note, a three-bar strength meter, and "Practice this", which opens the intake with the skill filled in through `/session?topic=`). **Those items are placeholders** (`FOCUS` in HomePage.tsx) until the tutor records skills per student. On phones the side card comes before the list. Sessions store no recap yet, so `lib/home-stats.ts` (unit-tested) uses the tutor's last sentence, and untitled sessions take their name from the student's first ask. `GET /api/sessions` names its columns, so a column added to the schema before its SQL has run cannot take the list down (event_seq did, Sept 16). A fixed-height `<button>` centres its content vertically, and `FooterWave`'s canvas sizes itself to its parent through an inline style, so give it a sized wrapper rather than height classes. The page grid uses `minmax(0,1fr)` columns at every width: a bare grid column grows to fit long session titles and pushed the cards off a phone screen.
- Old `LeftNav`, `Sidebar`, `SubtitleBar`, `FloatingPanel` are gone. Onboarding was rebuilt on Sept 20 2026 (see "Onboarding").
- **Sign-in** (`app/signin/page.tsx`, Sept 14 2026): layout from shadcnui-blocks `login-04` (narrow centred form beside a tall rounded panel), built from our own parts: shadcn `Field` / `Label` (added from the base-nova registry, `components/ui/field.tsx`, `label.tsx`), `Input`, `Button` (outline for Google, the glossy black default `btn-gloss` variant squared to 10px for submit, with `.btn-gloss-lift`: rises 1px with a deeper shadow on hover, settles when pressed), 40px controls in a 320px column, the `Wordmark` tile, and the hero's `DitherWave` filling the whole panel edge to edge in its `pattern="swirl"` mode, coloured with the voice wave palette (`VOICE_BLUE` exported from `components/session/VoiceWave.tsx`: #73adff, #2988f2 and a little near-white), the scribble mark and Sora wordmark in ink at the bottom left (lg+ only, still under reduced motion). One screen for sign in and sign up (`?mode=signup`), `?error=` codes mapped to plain messages, same-site `?callbackUrl=` honoured, client validation with focus on the first bad field, show-password toggle, proper `autoComplete`. The block's react-hook-form + zod were left out: three fields did not justify three dependencies. No "forgot password" link because there is no reset flow yet.

## Onboarding (Sept 20 2026)
Mateo asked for the five-step onboarding to be scrapped ("a first, ever version") and rebuilt as the first step toward a tutor personalised to each student, with separate paths for parents and students. The research behind the design is the "Knowing the Student" artifact (https://claude.ai/code/artifact/19c52738-11e8-499d-b4fd-af8e1fdc7a75): self-report predicts ability at about r = .29, a fifth to nearly half of teenagers straightline rating scales, and Aristotle's nine-question onboarding collects no ability data either. So the flow asks only what cannot be observed, and the tutor learns the rest by teaching.
- **The flow** (`app/onboarding/page.tsx`, in `OnboardingFrame`: the sign-in page's layout, a narrow column beside the swirl panel with the tutor's notepad on it, shared with `/welcome`, so signing up, setting up and the first session's brief read as one thing). First screen: student or parent. A **student** answers a name and a grade and goes on to `/welcome` (next bullet). Both branches end on **"What are you into?"** / **"What is Ana into?"** (`InterestChips`): twelve chips with an emoji each, up to six, and **"Add your own"**, because a kid whose whole life is skateboarding should not have to answer "Sports" (their own chip is stored as a plain label like any other and carries a neutral ✨). At the cap the rest go quiet with "Six is plenty. Take one off to swap in another." rather than silently refusing. It was on `/welcome` until Sept 20; Mateo asked for it as its own step, with emoji and custom entries, so `/welcome` now asks for nothing and only briefs. A **parent** answers the child's name, the grade, and "What's going on with math right now?" (`CONCERNS` in `lib/onboarding.ts`: falling behind, a test, homework, more of a challenge, not sure yet; plus an optional note of up to 300 characters behind "Add a note"), then a handoff screen ("Ana is all set. Hand this to Ana when they're ready.") with Start a session now / I'll do it later. Switching roles on the first screen starts the answers over; picking the same role keeps them. Cut on purpose: the four preference sliders (the prompt already told the tutor to trust what it sees over them, and 23–46% of teenagers straightline that format), the free-text "anything else", the voice picker (Settings has it), and the step counter (length only costs completion when it is perceptible).
- **The brief before a first session** (`app/welcome/page.tsx`, Sept 20 2026). Mateo did not want a new account "thrown into a session". `/session` sends anyone with no sessions yet to `/welcome` and the brief comes back with `?ready=1`, so it reaches all three cases: the student who just signed up, the child a parent set up who opens it later, and a parent trying it themselves. The parent's handoff button goes to `/welcome` directly (routing it through `/session` only bounced straight back) and holds a pending state, because a click that navigates must show something at once. `NewSessionClient` opens the intake anyway after `OPEN_ANYWAY_MS` (5 s) if either lookup is slow or blocked, and a late answer then no longer redirects: a blocked request used to leave the student on "Setting up your session…" with nothing else to look at. On it: three things to know (you talk and it talks back, it writes as it explains, it won't just hand you the answer), a **mic check** (`MicCheck`: "Turn on the mic" asks the browser with context instead of the session asking out of nowhere, then the voice dock's own `VoiceWave` shows the student their voice and the copy turns into "We can hear you"; declining leaves the session's composer, and the copy says so), and a quiet `FREE_LINE`. **It asks for nothing and saves nothing**: what they are into moved into onboarding on Sept 20, so this screen only briefs. `FREE_LINE` is "Free while Chalk is in beta." because the landing's pricing says the same and nothing counts sessions yet; Mateo floated "Your first 5 sessions are free", which needs the landing's Free plan to match and a counter behind it.
- **What interests are for.** Stored as `onboarding.interests` (plain labels, at most six) and read by the prompt as "Likes: Sports, Space. Use these for examples when the math has a story, lightly (names and settings, not statistics), and never say that you are doing it." The last clause is deliberate: per Walkington and Bernacki the effect runs through a mechanism students do not consciously notice, and their replication's manipulation check failed, so anything that asks the student to notice the personalisation optimises the wrong thing.
- **Staged lessons** (`lib/staged-lessons.ts`, pure, tested; replaced the flatter `starter-topics.ts` on Sept 20 after Mateo called a list of bare problems sloppy). Ten lessons we picked, each carrying a **rehearsed plan**, not just a topic: the catalog skill, where to open, three problems in order (warm-up, the real one, a stretch), the mistake to expect, and the first board move named as a real tool (a test fails if it names a tool that is not declared). `lessonPlan()` puts that into the session's instructions through `intakeInstructions`, so a picked lesson runs like something prepared; the plan ends by telling the tutor to drop it if the student turns out to have their own homework, and the teaching rules still decide how much help to give. `lessonsForGrade` leans to the student's grade and always returns enough to choose from. `lessonTopic` is what fills the box: "Fractions: 1/2 + 1/4".
- **Where the lessons are offered: the intake, and only the intake.** They were briefly an onboarding step too, which Mateo cut on Sept 20 ("why is this shit still here?"). That step also **hid the bubble**, since prefilling the box made the intake non-empty, so he never saw the thing he had asked for. Nothing in onboarding writes a topic now. In the intake they sit **outside the window**: a bubble beside the dialog with a tail pointing at it (Mateo drew this twice; four tiles crammed inside the window read as clutter). Below `lg` there is nowhere to put a bubble, so a quiet "Nothing in mind?" keeps them one tap away instead. `TopicTiles` is the shared control: emoji in its own tinted square, the topic, and what you will actually do, as a radio group with arrow keys. One column: two columns at the onboarding column's 400px wrapped every blurb onto three lines and left the tiles uneven.
- **A parent's answer is a lead, never a fact.** It is stored in `user_profiles.onboarding` (`{ by, concern?, note? }`, jsonb; `lib/db/sql/2026-09-20-onboarding.sql`, applied to the Neon main branch on Sept 20 2026, older profiles hold `{}`) and reaches the prompt through `profileLines` as "A parent set this up and said a test is coming up. Treat it as a lead to check for yourself, not a fact, and never repeat it to the student." (`onboardingProfileLine`, tested). A student's own signup adds nothing. `PUT /api/onboarding` writes the record only when the body carries one, so the settings page's saves, which do not, cannot erase it. Both live-session routes pass the raw column through `StudentProfile.onboarding`.
- **Grades are grades now.** `GRADE_OPTIONS` stores "5th grade" … "12th grade", "College / University" and "Self-learner" (shown as "Not in school"); `registerForGrade` reads 5th as elementary, 6th–8th as middle, 9th–12th as high, and `lib/onboarding.test.ts` checks every one (10th must not read as 1st). `STUDENT_LEVELS` in `profile-options.ts` offers the same list in Settings; the old "Middle school (6–8)" and "High school (9–12)" values are matched as `LEGACY_LEVELS` but not offered again. Until now a 5th grader had no option but "Middle school".
- **The tutor's notepad.** On the panel, and on a phone on the handoff screen, `TutorNotesBoard` (`components/onboarding/`) shows the tutor taking notes as the answers come in, in the settings page's handwriting on its dotted board (`BOARD_DOTS` moved to `components/app/board-dots.ts`): Name, Grade, then for a student "Next: Ask what they're working on", and for a parent "Parent says: A test is coming up" and "Note to self: Check this for myself". That last line is the rule made visible. `onboardingNotes` (pure, tested) builds them; lines are keyed by label, so a name being typed updates in place instead of rewriting on every keystroke.
- **Craft.** One question a screen. The question sits at the same y on every screen (a fixed 44px slot holds Back). Screens rise out of a 4px blur on a bounce-free spring with their pieces 70ms apart and leave faster and softer (`motion/react`; opacity only under reduced motion, nothing on first paint). Chips are a real radio group (`OptionGrid`: roving tabindex, arrows and Home/End move the selection, sky-soft fill with a sky check that fades, scales and un-blurs in). Every target is at least 44px; focus moves to the question on each screen and to the input on the name screen; the primary button is the sign-in page's glossy black one at 10px, disabled until the screen is answered. Measured headless on Sept 20 at 1440 and 390 with a throwaway account (deleted after): no horizontal overflow, selected-chip text 16.7:1, helper text 6.7:1, the question at 211px on a laptop and 201px on a phone on every screen.
- **Not done:** one account is still one learner (a parent with two children needs a `learners` table and repointed foreign keys; the record is shaped to move), there is no invite link for the handoff, the notepad does not appear on a phone until the handoff screen, nothing counts sessions toward a free allowance, and nothing seeds `learner_skill_states` yet: that is the prerequisite graph and the in-lesson diagnosis the artifact plans next. Measured on Sept 20 at 1440×900 and 390×844 with throwaway accounts (deleted after): `/welcome` fits a laptop without scrolling, the fake-device mic check reaches "We can hear you", interests land on the notepad and in the saved record, the intake shows four grade-picked starters, and bare `/session` redirects to `/welcome` while `?ready=1` stays.

## Before the session starts (Sept 15 2026)
"New session" no longer drops the student straight onto the board. `/session` (`app/session/page.tsx`) shows `components/app/SessionIntakeDialog.tsx` first: what they need help with, and a photo or PDF of the work if they have one, through the session screen's own upload box (`FilesPanel`, exported from `FilesPopover.tsx`, which now takes a `hint` line so this window doesn't say "drop them on the board"); the window itself is a drop target too. The language sits as a quiet pill in the footer, over a pale strip of the landing footer's shader (`FooterWave` now takes a palette plus `edge`, `swing` and `reserve`, so a 124px strip still reads as a wave rather than a gradient), and **defaults to English**; 13 are listed in their own names. A first version also asked where they are with it, what it's for and how long they have, with example chips under the text box: Mateo cut all of it on Sept 15 as overwhelming for a middle schooler, so keep this window to the two questions. The QA and preview entry points (`?debug=1`, `?qa=1`, `?mock=1`) skip the dialog and start as before, so `/api/dev/qa-login` still works.
- `lib/session-intake.ts` owns the answers and the two texts built from them: `intakeOpeningMessage` (the student's own first turn) and `intakeInstructions` (the language rule, the quoted topic, the attachments, and "ask nothing about what they want to work on"). Unit-tested in `lib/session-intake.test.ts`.
- The handoff: the text fields go through localStorage keyed by session id (they survive a reload), the files stay in memory for the tab because base64 images are far too big for localStorage, and `takeIntake` reads once, clearing the store but caching the result so React's double-mount in development cannot lose it. `setActiveIntake` hands the answers to the live client, which reads them as the socket opens.
- The session then dives in: `app/api/live-token/route.ts` appends `intakeInstructions` after `buildGeminiInstructions` (so `lib/tutor-prompts.ts` is untouched) and returns an empty greeting, and `lib/gemini-tutor.ts` sends `GeminiLiveSession.sendOpening(text, files)` instead of `sendInitialGreeting`, so the tutor's first sentence answers the problem. The route only trusts fields it recognises and caps the free text at 600 characters.
- The session takes its name from the topic, the photos ride into the first turn as attachments, and the opening line is written into the transcript as the student's own, so the screen shows why the tutor started where it did.
- **PDFs (Sept 16 2026).** Gemini Live reads pictures, not PDFs, so an uploaded worksheet used to reach the tutor as "Skipped unsupported file type". `lib/worksheet-pages.ts` renders the first six pages of a PDF to 1600 px JPEGs with pdf.js (`pdfjs-dist` 6.3.289, Apache-2.0, a lazy chunk loaded only when a PDF is attached; its worker is vendored to `public/vendor/pdf.worker.min.mjs` by `npm run vendor:pdfjs`, and `PDFJS_VERSION` must match). The page renders them before a session starts and before a mid-session upload is sent (`prepareFiles`), `buildFileParts` sends them as "Page 1 of 3" pictures, and every upload is recorded as a frame (`upload: File 1, page 2`), so the review shows the worksheet. GPT-Live still gets the PDF itself (`input_file`). `/dev/pdf` renders any PDF with sizes and timing (a three-page worksheet: 176 ms, 198 KB). pdf.js declares Node 22 or later but runs only in the browser here; npm just warns on Node 20.
- **Not wired yet:** the GPT-Live path. It receives the opening turn through the same message, but `app/api/live-session/route.ts` does not carry the language block, so a session on `?provider=openai` may answer in English.

## The tutor's pet (Sept 19 2026, prototype)
Mateo wants a small character for the tutor, since the app is for middle schoolers: a blue pixel blob with eyes in the voice wave's blues, moving, with a bubble when it thinks. **Mateo chose the rounded square** (Sept 20). `components/board/TutorPet.tsx` is the body: raw WebGL, a signed distance field for one of three outlines (`shape`: circle, rounded square, or a squat triangle with a soft top; the bean-shaped mockups were rejected, and the others stay only for the lab), wobbled by slow sines on the angle, squashed and leaned by a state machine on slightly underdamped springs, so every change of pose overshoots and settles (`state`: idle holds its pose and only the eyes move, glancing and blinking; listening leans toward `look` with wide eyes; thinking sways while the eyes scan; speaking pulses and bounces with `level`; writing leans and jiggles like scribbling; happy hops with a mid-air wiggle, a landing squash and smiling eyes; hello rocks side to side; puzzled tilts with one eye narrowed; surprised stretches tall with wide eyes; sleepy droops with slow blinks; arrive pops in from small), with one eye drawing in every state: a 6 by 6 block pill with a two-block glint (eight blocks tall read too tall, Mateo, Sept 20; the anchor sits on a block corner, so the height only moves in twos), drawn upright in screen space and snapped to whole blocks, so it rasterizes identically every frame and only its anchor follows the body's lean and squash; two lids clip it in whole blocks (the top lid comes down for squints, sleepiness and blinks, the bottom lid rises for a smile, over fully round ends so the cap is an arch), looks shift it by whole blocks, and wide eyes gain one block of height. Eyes that changed drawing per mood, or that rotated and stretched with the body, read as jagged and broke the character's continuity (Mateo, Sept 20), an outline counted in blocks rather than measured as a distance (`bodyAt` asks the 24 blocks around this one): a block is ink when any block touching it, diagonals included, is outside, which is exactly one block everywhere and can never gap, and the block behind that is tinted 30% toward the ink, so the edge carries about a block and a half of weight without a second line. Only blocks within three of the edge ask. A distance rule cannot do this: at two blocks it read fat, at one it gapped where the corner staircase runs diagonally, and at one and a half it flipped between one and two blocks as the wobble crossed the grid, so the edge pulsed while the pet moved (Mateo, Sept 20, "in the animations, the border looks weird"). Measured over ten frames of thinking: the edge holds 2.42-2.52px against the distance rule's 2.88-3.44px swing, a Bayer-dithered fill on its own grid of double-size blocks that churns (a slow fbm warps the one that is drawn) because one-block dither at a crawl read as noise, not a gradient (Mateo, Sept 20: bigger pixels, more movement); the noise is stretched over the ramp so the bands show, and the lightest tone is only a pale blue, since a near-white block read as a smudge, two big pixel eyes with a one-block glint that blink every few seconds, and a transparent canvas so it sits over anything. The simulation runs every frame but the body is drawn 24 times a second, so it still reads as drawn frames: at 60 fps the wobbling outline crawled and read as nervous jelly (Mateo: "the movement is a bit weird"). Idle holds perfectly still (Mateo, Sept 20): no breath, no bob, no wobble and no stretch, so the silhouette is the exact rounded square and only the eyes move. The dithered fill still drifts under it. About 48 blocks across whatever `size` (30 was too coarse for the eyes and the motion, Mateo, Sept 20); `reduceMotion` freezes the clock but keeps blinks. Lessons: `half` is a reserved word in GLSL ES and the shader failed silently until the compile log was read; the wobble must be smoother than the block size or single blocks poke out of the outline; anything smaller than a block (the glint) has to be sized in blocks. `/dev/pet` (`app/dev/pet/PetLab.tsx`, dev only, public in `proxy.ts` in development) shows every shape and state with a bubble (dots while thinking, "Your turn", "Nice!"). **What it says** is `components/board/PetBubble.tsx`: a speech bubble with a tail, and a thought bubble that is a real cloud with two dots trailing back to the pet. The cloud is generated for the size the bubble ends up (`lib/cloud-path.ts`, pure, 10 unit tests): five to eighteen circles sit round an oval inside the box and the path is the true union of the oval and the circles — each circle keeps the run of itself that neither another circle nor the oval covers, so the outline is real arcs meeting in **notches**. The oval has to be part of the union: once the scallops are small enough to leave its middle uncovered, the ring of circles has a hole and the hole's rim gets traced as outline (a test checks no arc runs through the body). Two ways of drawing it were wrong, both worth not repeating: sampling the union and smoothing the samples into one curve rounds the notches away and comes out a melted blob (Mateo, Sept 21, "the cloud doesnt look great"), and the notches are most of what says cloud; tracing circle to circle assuming neighbours self-intersects once the circles differ in size and draws rings inside the cloud. Billows are placed by **arc length** round the ellipse, not by angle: a flat ellipse covers far more ground per degree along its sides than round its ends, so equal angles bunch them at the ends, leave gaps along the top and break the union into pieces; the count then grows until every neighbour touches. A scallop is about 0.3 of the box height, capped at 0.2 of the width — at 0.4 a small bubble was two big circles glued together, a peanut with a dent (Mateo, Sept 21, "how does that look like a cloud"): the scallops have to be clearly smaller than the oval they sit on — and runs 1.08 at the top of the box to 0.86 at the base; the cloud reads from clean geometry, not randomness, so the size jitter is only 8%, both deliberately not pixel art (the pet is the character, the bubble is the app talking, in the app's own type and hairlines). It grows out of the pet — the transform origin sits at the tail — on a slightly underdamped spring, its words arrive one at a time (a word every 50 ms, the whole reveal capped at half a second so a long line speeds up instead of dragging), it resizes on a spring when the line changes, and thinking is three dots on a staggered loop. Reduced motion keeps opacity only and holds the dots at three depths. `side` is top, right or left and `align` picks the end the tail sits at; the caller puts the pet and the bubble in one positioned box. **The pet is 120px** everywhere (Mateo, Sept 20): smaller and it reads as an icon rather than a character. At that size a bubble beside the pet needs about 340px of room, so on a phone, or in any narrow panel, the bubble goes above it instead — a side bubble is positioned absolutely, so it does not widen the page and overflows silently off the screen edge.
- **Where it goes is not decided.** `/dev/pet-places` (`app/dev/pet-places/Places.tsx`, dev only, public in `proxy.ts` in development) puts the pet on every candidate surface at its real size: on the session dock, on the board, on the home page's start card, in the landing hero demo, at the welcome page's mic check (reacting to the real microphone), and in waiting and empty states. What the study showed: on the dock it reads as the tutor's face beside its voice and never touches the board; on the board it lands in the student's work, on tldraw's badge, and beside the pen cursor and its name tag, so **the tutor would have two presences at once** — that collision has to be settled (the pet carries the pen, or the pet stays off the board) before anything ships there; on the home card the pet has to sit above the wave's crest or it is blue on blue. Nothing is wired into the app yet.
- The plan for the session is still for it to replace the tutor's pen cursor as its presence on the board (`TutorPenOverlay.ts`), driven by `tutorActivity`, `isTutorSpeaking` and the audio analyser the dock already uses.

## GPT-Live protocol facts we verified (do not relearn these)
1. The Live model's clock is driven by **inbound audio**. With no mic track nothing happens: appended context is never injected and the model never speaks. Text-only QA sessions send faint synthetic room tone (`createSyntheticMicStream`).
2. `session.instructions.append` alone never makes the model speak. `session.commentary.append` does (a near-verbatim paraphrase within ~1 s). The greeting and the resume line are sent as commentary.
3. Client-triggered backend turns work: `response.item.create` (user message or attachments) + `response.create` → the Live service creates a delegation, the voice model bridges ("let me put that on the board"), then speaks the backend's text. Typed messages and file uploads use this path.
4. Tool loop: nested `response.output_item.done` (`function_call`) → execute on the board → `response.item.create {function_call_output}` → after the nested `response.completed`, send exactly one `response.create`.
5. Sessions expire after about 2 hours (`expires_at` in `session.started`). Voice and voice instructions are immutable per session; backend settings can change with `session.update`.
6. The voice model cannot see images. Photos and PDFs go to the backend as `input_image` / `input_file` items.
7. The SDK (`openai@7.15`) has typed Live events in `node_modules/openai/resources/live/live.d.ts` and a WebSocket client (`openai/resources/live/ws`) that is handy for Node probes.

## Voice provider switch (Sept 2026)
Two live stacks share one interface (`TutorClient` in `lib/tutor-provider.ts`: `start/end/setMuted/sendText/sendFiles` with `LiveTutorCallbacks`). The session page picks one at start:
- The in-code default is **gemini** for now (OpenAI account out of credits, Sept 2026). `NEXT_PUBLIC_TUTOR_PROVIDER=openai|gemini` in `.env.local` (and on Vercel, build-time) overrides the default; `?provider=gemini` or `?provider=openai` on a session URL overrides it for that tab. The session chip shows a small "GEMINI" tag when Gemini is active. To go back to GPT-Live by default, flip the fallback in `resolveTutorProvider` or set the env var to `openai`.
- OpenAI path: `lib/live-tutor.ts` + `app/api/live-session/route.ts` (unchanged).
- Gemini path: `lib/gemini-tutor.ts` (adapter that speaks `LiveTutorCallbacks`) → `lib/gemini-live.ts` (WebSocket BidiGenerateContent client, resumable) + `lib/audio.ts` (mic capture at 16 kHz, PCM player with an AnalyserNode for the voice wave). `app/api/live-token/route.ts` composes the Gemini prompt (`buildGeminiInstructions` in `lib/tutor-prompts.ts`: voice persona + the backend teaching rules, for one model) and the voice (`geminiVoiceFor` maps the OpenAI voice choice to a Gemini prebuilt voice); `{ configOnly: true }` returns prompt + voice, a plain POST also mints a one-use ephemeral token.
- Ephemeral tokens only work on the `BidiGenerateContentConstrained` WebSocket method with `?access_token=`; the plain `BidiGenerateContent` method closes with 1008 "unregistered callers". Verified 2026-09-14 with a handshake probe.
- **Which Live model (Sept 17 2026).** `LIVE_MODELS` in `lib/gemini-live.ts`: `3.1` = `gemini-3.1-flash-live-preview` (still the default), `3.8` = `gemini-3.8-live`, `3.8-thinking` = `gemini-3.8-live-extended-thinking`. `resolveLiveModel` picks one: `?live=3.8` on a session URL for one tab, else `NEXT_PUBLIC_GEMINI_LIVE_MODEL`, else the default; a full model id passes through, so a model released later needs no code change. The session chip shows which ran ("gemini 3.8") and `session.started` records it.
  - **What the API says.** Google now calls 3.1 a "legacy audio-to-audio preview model. We recommend updating to Gemini 3.8 Live"; `gemini-3.8-live` is stable and "the default for most low-latency voice agent experiences", `gemini-3.8-live-extended-thinking` stable and "recommended when higher background reasoning is required". Both are 131k in / 65k out.
  - **What 3.8 adds, and what held up here (Sept 17 2026).** Google's announcement lists asynchronous function calling, visual grounding, alphanumeric precision, 97+ languages and incremental content updates, at $0.005 a minute of audio in and $0.018 out (a 30-minute session lands near $0.40, against $1.60–2.10 on GPT-Live). Tested on our own stack with `scripts/live-model-probe.ts`:
    - **Async tools did nothing for us.** `behavior: "NON_BLOCKING"` on every declaration plus `scheduling` on the responses is accepted, but with an 8 s board the model still said nothing until the results arrived, on v1alpha and v1beta alike. No dead-air win; revisit on a later model.
    - **`enableAffectiveDialog` does not exist in the API.** Rejected at `setup` and inside `generationConfig`, on both versions ("Cannot find field"), though the guide lists it.
    - **`proactivity.proactiveAudio` and `mediaResolution` are accepted** on our v1alpha endpoint, but 3.1 accepts them too, so acceptance proves nothing. `MEDIA_RESOLUTION_HIGH` made board reading no better and pushed first audio from 4.1 s to 8.5 s.
    - **Board reading is not visibly better.** Asked to read a real board export (heading, a substituted slope formula, a graph with a rise-over-run triangle, a student attempt, a number line), 3.1 and 3.8 both read it back correctly.
    - **No thought summaries.** Still nothing with `includeThoughts`, so the replay cannot show reasoning.
  - **What our own probe found** (`scripts/live-model-probe.ts`, one student line, the real prompt and all 40 tool declarations): `gemini-3.8-live` works, calls its tools one at a time instead of in a single batch, draws before it speaks (first audio 2.9–5.3 s against 3.1's 1.9 s) and finishes the turn sooner (10–12 s against 15 s). `gemini-3.8-live-extended-thinking` closes the socket with 1007 unless `thinkingConfig.thinkingLevel` is set, then answers "I'm sorry, a system error occurred" on v1beta whether or not our tools and prompt are attached, and on v1alpha (the endpoint the browser uses) sits silent for about 22 s before drawing: not usable for a live tutor. No model returned thought parts, `includeThoughts` or not.
- Gemini has no Responses-delegation split, so the whiteboard tools are called by the voice model directly. Expect it to draw less carefully than the OpenAI pair.

## Learning evidence and shared tutoring runtime (Sept 17 2026)
- **One pedagogical owner.** `TutorRuntime` in `lib/tutor-runtime.ts` owns the `TutorPolicy`, current assistance ledger, tutor-tool execution, cancellation, and typed learning events. `LiveTutorSession` and `GeminiLiveSession` receive the same runtime from `app/session/[id]/page.tsx`; they are transports for audio, transcript, and tool messages, not separate teaching brains. Reconnects and provider replacement therefore keep the current skill, attempts, signals, and help history.
- **Canonical skills.** `lib/skill-catalog.ts` is the small, stable catalog for math the deterministic checker can judge. Resolution is exact over a key, label, or explicit alias. Always store `rawSkill`; an unknown label stays unresolved instead of creating a permanent skill. Add a catalog alias only when it is unambiguous, and keep the SQL seed in `lib/db/sql/2026-09-17-learning-evidence.sql` synchronized.
- **Assistance is evidence.** The model calls `record_teaching_move` before H1-H5 help and for deliberate H0 independent checks. Observable moves have minimum levels enforced in code: focusing question H1, point H2, strategy hint/counterexample H3, shown step H4, worked example H5, independent check H0. The runtime and server both use the maximum of the declared level and observable moves. Alias differences such as “solving two step equations” and “two-step equations” share one ledger. Missing help falls back to at least H1, never H0.
- **Attempts are immutable facts.** `check_answer` remains synchronous for the live loop. The runtime emits the problem, answer, raw/canonical skill, checker result, effective help, call id, and a stable problem fingerprint. `LearningRecorder` in `lib/learning-client.ts` batches and retries these events outside the voice path. A cancelled call is marked cancelled and reprojected; its audit row is not deleted.
- **Server trust boundary.** `/api/sessions/[id]/learning` verifies auth and session ownership. `lib/db/learning.ts` resolves the canonical skill, recomputes the math verdict and fingerprint, enforces move minimums, looks up assistance since the preceding attempt, inserts idempotently, and reprojects affected skills. Do not accept a browser-supplied verdict, canonical key, or H0 claim as authoritative.
- **Storage.** Apply `lib/db/sql/2026-09-17-learning-evidence.sql` once in Neon. It creates/seeds `skills`, `teaching_moves`, `learning_attempts`, and `learner_skill_states`. The prompt overview deliberately falls back to empty when the migration or database is unavailable, so evidence is shadow-first and a storage outage does not block tutoring.
- **Projection rules.** Pure rules live in `lib/learning-evidence.ts`: `building` has attempts but no success; `supported` is a correct H1-H5 answer; `independent_recent` is a recent correct H0 answer; `review_due` is independent evidence at least 24 hours old; `retained` needs correct H0 work on distinct problems at least 24 hours apart; `needs_revisit` is a substantive miss after success. `unchecked` and cancelled work do not count. A slip alone does not erase retention.
- **Consumers.** `lib/learning-overview.ts` reads projections and refreshes time-based `review_due`; `/api/learning/overview` serves it. Both live-session routes append its three-item learner brief to the teaching prompt. The brief is a soft guide: the student's explicit goal wins, and at most one relevant warm-up is offered. `HomePage` reads the same overview; there are no fabricated focus rows or mastery claims.
- **Closing behavior.** An independent exit check is offered, not forced. If the student declines or needs to leave, the tutor closes warmly. Do not restore the old “Never end on I don't know” rule.
- **Tests.** The catalog, projection, runtime, server validation, retry queue, overview, prompt contract, provider sharing, and home view model all have focused unit tests. `npm test` was 251/251 after this addition. Full `npm run lint` still reports the five documented pre-existing errors in `Magnet`, animate-ui tabs/highlight, and `dot-pattern`; lint all touched files directly when checking this feature.

## Tutor voice speed (Sept 14 2026)
- **The control** (`components/session/SpeedControl.tsx`, `useTutorSpeed`) is a round button left of Files whose dial icon (`SpeedGauge`: a 240° arc and a needle on a filled hub, in lucide's stroke weight; picked over lucide's own gauge, whose needle floated, from five drawn variants) points at the current speed. The needle turns by a CSS transition with a slight overshoot and leans a few degrees on hover. It opens a Base UI popover that grows out of the button (origin-aware scale from 0.86 through a 6px blur, 260 ms on the iOS drawer curve in, 150 ms out; the title, slider and labels stagger in with `@starting-style`). Inside is a Base UI slider with five named stops, `TUTOR_SPEEDS` in `lib/voice-settings.ts`: Slowest 0.7×, Slow 0.8×, Normal 1×, Fast 1.2×, Very fast 1.4×. The slow side has finer steps on purpose. The thumb slides between stops, grows while held, and springs a little as it lands; dots under the sky fill turn white; the stop names are clickable; Reset fades in away from the default. Focus lands on the slider (arrows, Home/End) and Escape returns it to the button. Reduced motion keeps only fades.
- The default is **Slow (0.8×)**: Mateo asked for slower than the first 0.9× default and for names instead of numbers. The stop id is remembered per browser in localStorage (`tutor_speech_speed`) and a change applies mid-sentence.
- Neither live API has a speaking-rate setting: Gemini's `speechConfig` is voice and language only, GPT-Live has none (only the older Realtime API had `speed`). So the client time-stretches Gemini's PCM with pitch kept. `AudioPlayer` in `lib/audio.ts` runs **Signalsmith Stretch** (MIT, WASM in an AudioWorklet) and keeps its own model of the node's timeline (an anchor: this input position is heard at this context time, moving at this rate), because the node's playhead runs on past the end of the buffer during silence; each burst of speech after a pause re-anchors it. Captions pace by `getPendingSourceMs()` (original-audio time) so a slowed voice keeps its caption in step. If the worklet cannot load, or throws later (`processorerror`), playback falls back to plain buffers at 1×.
- The pill shows only on Gemini sessions (`TutorClient.setSpeechRate` is optional). GPT-Live's voice arrives as a live WebRTC track; slowing that would pile up delay.
- **The library is vendored, not bundled.** `npm run vendor:stretch` (`scripts/vendor-stretch.mjs`) copies it from the devDependency into `public/vendor/signalsmith-stretch.mjs`, and `lib/audio.ts` imports that URL with a `webpackIgnore` comment (`/vendor` is a static path in `proxy.ts`). Bundled by Turbopack, the worklet never became ready (4 s timeout); the same file unbundled was ready in about 10 ms. The script also **patches a streaming bug in 1.3.2**: the worklet's buffer copy loop drifts across many small appended buffers and throws, so 40 ms chunks (what Gemini sends) killed the node within about 150 ms, silently. The script fails if those lines change; re-check the patch before upgrading, then bump `?v=` in `STRETCH_URL`.
- **Free audio QA:** `/dev/voice` (dev only) streams a speech sample into the real `AudioPlayer` in 40 ms chunks at twice real time, the way Gemini delivers a turn, and reports heard duration against expected, the engine mode, and interrupts; `window.__voiceLab` has `play`, `interrupt`, `setRate`, `stats()`, `debug()`. The sample is local only (`public/dev/` is gitignored): make it with the `say` and `afconvert` lines at the top of `app/dev/voice/VoiceLab.tsx`. Close the lab's player on Fast Refresh (it does); a closed context streams silence.
- Engine check on `/dev/voice` (Sept 14, 8.5 s sample, before the stops were named): heard 8.48 s at 1× (8.52 expected), 9.40 s at 0.9× (9.46), 10.58 s at 0.8× (10.65), 7.08 s at 1.2× (7.10); a switch from 1× to 0.8× mid-sentence 9.80 s (9.84); an interrupt silent within 400 ms; the next play after it complete. Speech starts about 120 ms later than before (the engine's latency). Not yet tried in a real Gemini session or a production build.

## Settings (Sept 14 2026)
- `app/settings/page.tsx` is written as a note to the tutor, not a form. The left column is three short passages in large type with blanks to fill in: "Hi, I'm [name]. I'm [in high school]. Right now I'm working on [text]", "When I'm stuck, [give me a hint first]. Take it [at a steady pace], start with [an example], and keep it [friendly]", and "Use [Marin] as my tutor's voice, and talk [slowly]". Blanks are sky-tinted fields with text in the app's own sky (`--lp-sky` #3d9cff, the sidebar's accent blue; Mateo chose it over a darker text blue, knowing it is about 2.7:1 on the tint); the choice blanks are Base UI `Select`s whose menus grow from the phrase and give each option a one-line description. Account and sign out sit quietly under the passages. The first rebuild (label-and-control rows) was rejected as too close to the old page.
- The right column (sticky on desktop, below on phones) is **What your tutor reads**: the exact lines `profileLines` builds for the prompt, with friendlier key names ("How to explain" for Register, "Working on" for the context, "Preferences" for the stated-preferences line, which `profileLines` only sends when a preference is off Balanced), in the board's handwriting (`.lp-hand`) on a dotted board. A line re-writes itself with a clip-path wipe when a choice changes it (typed lines don't, so typing doesn't flicker). Under it, **What it remembers** lists the tutor's saved notes (`/api/profile/notes`, newest first) with Clear behind a confirmation dialog.
- Phrases and option copy live in `lib/profile-options.ts` (unit-tested): level values are the exact strings onboarding stores and `registerForGrade` parses (`matchLevel` tolerates hyphens and case; a numeric level reads "in grade 11"); preference phrases say what `profileLines` tells the tutor ("explain it to me" means more explaining, never answers).
- **Everything saves as it changes.** Choices save at once, typing after 700 ms or on blur, a pending edit flushes on leave (`keepalive`). Each save PUTs the whole profile to `/api/onboarding` through a promise queue that always sends the newest snapshot, so the last change wins, and keeps `learningPrefs` keys the page doesn't edit (an older onboarding on another branch stored `supportPreferences`). The top bar shows Saving (only past 400 ms), Saved, or Couldn't save with Retry. Speaking speed is per browser via `useTutorSpeed`, shared with the session's speed control.
- Removed from the old page: voice preview buttons (OpenAI TTS, failing and not the Gemini voice), decorative voice colour bars and sample lines, the Save button, uppercase eyebrows, and the false "adapts immediately" copy (the profile reaches the tutor when a session starts).

## Landing page (Sept 2026, rebuilt on the real UI Sept 14, product page below the hero Sept 16, reviews and pricing Sept 20)
- Lives in `components/landing/`. `LandingPage.tsx` composes: `Header` (a 60px bar that morphs on scroll, through one Motion spring, into a floating 48px liquid-glass rectangle with a 16px radius: a web approximation made of backdrop blur and saturation, white edges, a sheen and a soft shadow, after `references/Screenshot 2026-09-14 at 3.45.11 PM.png`, kept rectangular on purpose; a 19px Sora wordmark; nav links whose sky scribble underline draws itself on hover through `.nav-link` and `.nav-scribble`; the glossy lift CTA), `Hero` (the swirl across the whole hero with its own settings, `HERO_SHADER_DEFAULT` in `ShaderTuner.tsx`, the field picked by Mateo in the tuner on Sept 15 (the sign-in swirl's colours, 7 shades and 3px pixels, scale 2.55, warp 0.66), calmed on Sept 16 after he found the full-strength blue "too much": speed 0.04, lightness +0.14, a 50% white wash and a 560px bottom fade; the sign-in panel keeps the plain `SWIRL` preset; a masked version under a 70% white wash was too faint (Mateo, Sept 14); the subtitle and the header's nav links are ink so they read on the blue; a still frame under reduced motion. Double-click the hero's background, in development or on any build with `?tune` in the URL, to open `ShaderTuner`: a draggable liquid-glass panel (the `.glass` material plus the header's edge light) that changes pattern, animation, speed, scale, warp, lightness, shades, pixel size, the three colours, a white wash and the bottom fade live, with Reset and Copy values. Tuned values persist in localStorage `chalk.heroShader.v1`, this browser only. Double-clicks on links, buttons and the demo frame (`data-no-tune`) are ignored. The settings state lives in the hero's `Backdrop`, not `Hero`, so dragging a slider never re-renders the demo board) holding `SessionDemo` inside `HeroFrame`, which leans the app frame back 14 degrees, at 0.94 size, and stands it up as its top scrolls from 92% to 30% of the viewport, through `useScroll` motion values and a light spring, flat under reduced motion; the title is "A math tutor you talk to. It writes as it explains." at weight 500), then the Sept 16 sections (next bullet), then (until Sept 22, now replaced by `Ending`, see below) `Footer` (after the Sona UI and Rare UI footers: the wordmark, a one-line description and the copyright beside two link columns that only point at real sections and pages, then the sign-in panel's white wordmark (scribble and "chalk", 56 to 96px) sitting in a band of `FooterWave`, the dock's voice wave stretched into a horizon: the same Bayer dither, 3px pixels and voice blues, standing ripples sized in CSS pixels so they never stretch, a slow drift, a level that breathes like quiet speech, a lighter ridge behind, and a swell that eases toward the mouse over the footer. The wave rests in the room above whatever carries `data-wave-keep` (the wordmark) and the blue deepens smoothly toward #1d72dc behind it, so white measures at least 3.3:1 at every moment; clamping the surface to a floor instead made a flat line and a slab, so don't. The crests plus the ~55px soft edge always stay inside the canvas; it pauses off screen, and reduced motion gets a still frame that repaints on resize).
- **Below the hero (Sept 16 2026; reviews and pricing added Sept 20).** Mateo called the old sections "slop" and asked for a modern software-product page after four references (React Bits' Cloudlight template, Framer's Zova, Qarin and AgentLab); Cloudlight set the system. Everything under the hero sits inside `.lp-rails` (two vertical lines at the 1180px container's edges, shown from 1240px) and each white section opens on a dashed rule across the viewport (`.lp-rule`). **Every part of both is a CSS variable** (Sept 21, so they can be tuned live): `--lp-rail-w`, `--lp-rail-inset`, `--lp-rail-bg`, `--lp-rule-h`, `--lp-rule-bg`, declared in a `.lp` block right above the rules in `globals.css`. Solid is a plain colour in the `-bg` variable; dashed is a `repeating-linear-gradient` along the line (180deg for a rail, 90deg for a rule). Mateo asked for them thicker and more noticeable on Sept 21, then pointed at AgentLab (one of his original references) and asked for the whole grid dashed and slowly moving. So **both** lines ship dashed and they **crawl**: `@keyframes lp-rail-crawl` / `lp-rule-crawl` move the background by exactly one dash period (`--lp-rail-period`, `--lp-rule-period`), which lands the pattern back on itself so the loop has no seam, at `--lp-dash-speed` on a linear loop. Mateo tuned the values himself in the tuner on Sept 21 and they are now the shipped ones: 1px at **30% black**, dashes **12 on 6**, one period every **4s**, an **11px** node, and the rails **636px** from the centre. That last one matters: the rails sit 46px OUTSIDE the 1180px container on purpose, framing the page with air rather than tracing the content, which is why the media query is **1320px** and not 1240 (twice 636 plus a margin, or the left rail lands off screen). Change the inset in the tuner and check that breakpoint. Reduced motion stops both. Only the visible strip of a rail is ever rasterised, so an animated background on a 10,000px-tall element repaints about 780 pixels a frame. Wherever a rule crosses a rail there is a small square **node** (`--lp-node-size` 9px, `--lp-node-color`, filled with the page colour), drawn by `.lp-rails .lp-rule::after` as four background layers, an outer square and an inner fill for each rail, because one pseudo-element has to draw both; `calc(50% ± var(--lp-rail-inset))` centres a fixed-size layer on the rail at any viewport. Rails and nodes live inside that media query, so phones and narrow laptops keep only the dashed rule. **The node has to out-paint its rail**: `::before` paints before the container's children and `::after` paints after them, so the right rail was drawing straight over its own nodes while the left rail sat behind them, which looked like a dash running through some nodes and not others. The rails carry `z-index: 0` and the node `z-index: 1` (well under the header's z-40) so a node always wins. The first section under the hero (`Intro`) now carries a rule, so the grid opens with a line and two corner nodes directly under the hero. That first rule sits exactly where the rails' box begins, which left the top crossing with a rail going down and nothing above it (Mateo: "weird thing happening with the dashed lines"); the rails now start `--lp-rail-lead` (120px) above the container and fade in from nothing over that distance through a `mask-image`, so they emerge from under the hero and pass through the corner nodes. The card and row hairlines (`--lp-line`, `--lp-line-strong`) are both 9% since Sept 21, also his tuner values. **`.lp-railspan` is the shared box for anything that attaches to the rails** (Sept 21: Mateo asked for it on the Trio grid and the feature cards within an hour of each other, and two sessions wrote it twice before sharing one). It is `calc(2 * var(--lp-rail-inset) - 2 * var(--lp-rail-w))` inside the 1320px query, which is 1270px: strictly BETWEEN the rails, never on them, because a box sitting exactly on them covers the left rail (`::before` paints under positioned content) and is covered by the right one (`::after` paints over it), the same asymmetry that made the nodes look wrong. Below 1320px there are no rails to attach to, so it falls back to 1116px, the container less its two 32px gutters, which is the heading's own text column, and keeps the container's 20/32px gutters below that. `--lp-rail-ink` is the dashes' colour as a flat value, for lines that belong to this grid but are not dashed; the tuner emits it, so it follows the rule colour and opacity. `KeycapSlider` deliberately keeps its own inline width, and it is the FALLBACK that differs, not the geometry: it is a marquee that should cut at the screen edge below 1320px, where `.lp-railspan` would pull it into the container and cut it mid-air. It builds the same 1270px from the same tokens, `calc(2 * var(--lp-rail-inset) - 2 * var(--lp-rail-w))`, so tuning the rail width moves all three together; it hardcoded `- 2px` until Sept 21, which was the same number until the moment anyone touched the tuner. `--lp-rail-w` is declared outside the 1320px query for exactly this reason: a calc referring to an undefined variable is invalid and would collapse the width to nothing below the breakpoint. **The line tuner** (`LineTuner.tsx`) changes all of it live: Shift+L toggles it, `?lines` in the URL opens it on load, and it runs in development or on any build loaded with `?tune` or `?lines`. It writes the variables onto the `.lp` element, saves to `localStorage` (`chalk.lines.v1`) for that browser only, and its Copy button returns a `.lp { … }` block to paste straight into `globals.css` to make a setting the shipped one. It also drives `--lp-line` and `--lp-line-strong`, the hairline every card, row and border is drawn in. Nothing in it may be read during the server's render (the URL's query and the saved settings are both unknowable there), so a `useSyncExternalStore` hydration flag gates every read. Its chrome and controls are shared with the shader tuner through `tuner-parts.tsx` (`TunerPanel`, `Group`, `Range`, `Toggle`, `Segmented`, `Swatch`); both were checked open after that extraction. `Section.tsx` is the kit: `Container` (1180px, 20/32px gutters), `Section` (Sept 21: 56px top and 80px bottom, 64px and 112px from sm; the rule sits at the section's top so the top padding is the gap under the line, and 112px there read as a void, which Mateo circled), `Label` (Sept 21: the heading's own face and size, 32 to 50px Schibsted, in sky-deep at weight 400, so a section opens on two display lines, a thin blue one naming the section over a medium ink one saying the thing; the weight is an inline style because `.lp-title` fixes 500 from outside Tailwind's layers, where a `font-normal` utility on the same element loses the cascade. The 14px plain label was "vibecoded" to Mateo; a pill with a node-square mark was "worse"; this is what he asked for in words, "same font and size but in the blue color", "thinner". Nothing links from a label any more, so the chevron variant is gone. `Reviews` lost its "What people say" label for "Reviews", because at display size it repeated the title), `Title` (`.lp-title`: Schibsted at weight 500, -0.028em, 32 to 50px), `Lede`, `Caption` (bold lead, grey rest), `Reveal` (a 22px rise out of a 6px blur, once, at 20% in view) and `LandingMotion` (`MotionConfig reducedMotion="user"`, so transforms and blur stop under reduced motion while opacity still fades). `Band.tsx` (Sept 20) is the hero's field as a surface: `DitherWave` at `HERO_FIELD` (the hero's own settings) under a white wash, running only while in view, still under reduced motion, `z-[1]` so the rails never paint over it; full-bleed on its own, a tinted card inside a column. The order is the argument: `Intro` (rebuilt Sept 20 from Mateo's own mockup: no label, no heading of its own, just the page's one correction, because the copy was already a correction written as two matching paragraphs. "Homework help and tutoring is stuck in a chat box" sits in weight 400 ink (not grey, Mateo's call) and is crossed out by the page's light sky pen (#3d9cff, not the board's deeper strike colour, Mateo's call: here the line is decoration and `<del>` carries the meaning). The stroke is about 4.4px and is painted **over** the words, not behind them: a background paints behind its own text, so the letters cut the line into pieces and it read as half-transparent. The sentence is therefore set twice, the second copy in an absolutely positioned box the same size as the first with `color: transparent`, carrying the stroke, `aria-hidden` and unselectable; both wrap identically at every width (measured dx 0, dy 0 at 1440 and 390). Each sentence is its own flex row with its mark beside it rather than inside its line boxes, because a 44px mark in a 31px line box made the last line taller and pulled the two copies out of register on phones. The stroke is **timed, not scroll-linked** (his call, after a scroll-linked version): `whileInView` at 60% in view, a 0.5s wait, then 1.5s on a gentle ease-in-out `[0.37, 0, 0.3, 1]`, because the page's usual hard ease-out front-loaded the stroke and made it read as a snap however long it ran. Sampled: flat at 0% to 450ms, 25% at 900ms, 66% at 1200ms, 100% by 2100ms **as the page scrolls** (`useScroll` on the line itself, offset `["start 0.95", "start 0.45"]`, driving `background-size` from 0% to 100%; the stroke is an inline SVG data URI built in the file, not globals.css, with `box-decoration-break: clone` so every wrapped line gets its own, and `preserveAspectRatio: none` stretches it, which is why its swing has to be generous in the viewBox to read as a hand rather than a ruler). Reduced motion is gated by hand here: `MotionConfig reducedMotion="user"` only stops transforms, so a background that grows would still animate. The line ends with the ChatGPT, Claude and Gemini marks as one overlapping 44px circle cluster, stacked the way HeroUI's `AvatarGroup` does it in **clip** mode: rather than ringing each circle in white, the one underneath has a crescent cut out of it by a radial-gradient mask where its neighbour sits (44px discs, 12px overlap, 2px seam), so the page shows through the gap. The technique, not the package: `@heroui/react` ships its own token system and would be a second design system next to shadcn and Base UI. Each mark is painted by masking its shape out of a CSS background (`ai-marks.ts`, `fill` is any background value), so a flat brand colour and a gradient render through one path: Gemini is Google's four-colour mark, which is a cone around the centre rather than a line across it, so it is a `conic-gradient` (red at top, blue right, green bottom, gold left) and carries `scale: 1.2` because its four thin points read smaller than a solid mark at the same box. Shapes from Simple Icons, the icon data CC0, each trademark its owner's, nominative use with no endorsement implied; ChatGPT's black is the project's one ink. Under it, "Chalk is like the tutor you talk to over a video call" at weight 500, ending with the Chalk mark **drawing itself** start to finish: no tile behind it, 62px wide, its viewBox cropped to the ink (`1.4 4.4 18.2 13.6`, because the component's own 22x22 box carries whitespace that would only inflate the line height), animated with `pathLength` on the real stroked path from `ChalkMark`, which is `fill="none"` plus a stroke, so the draw follows the actual curve rather than wiping over a filled shape, then one grey paragraph. Both lines are 38px at 1440 and fit on one line each; 24px and two lines at 390. A wall of grey chat-answer steps stood beside this for one round and was dropped when Mateo redrew the section), `HowItWorks` (`components/landing/how/`, Sept 22: **replaces `AskBoard` and `Trio`, both deleted**; they showed the same thing twice and both only ever showed one linear equation). It is a **bento**, which Mateo chose over five other layouts that were built and deleted (a pinned scroll story, a camera gliding over one big board, a question index, a printed script, a session timeline: "too much", "too much scrolling", "Script sucks"). Then, over four rounds, he shaped it into **cards and nothing else**: no grid lines at all, no dividers and no frame ("we don't need those lines"). Each card is the page's own double border, `.lp-frame`'s recipe (a hairline, a 5px band, a white panel), **square like the grid** (its corner nodes came off on Sept 22), 16px apart. **Text lives inside the card** and **both zones are white** (a grey-blue tint read too dark and was cut): the picture above, the title and line below, split by a short dashed rule in the grid's dashes (`SPLIT`, a finer 6 on 4, lighter, still). The panel's inner edge is a real `border`, not an inset shadow: children paint over an inset shadow, so the picture half had lost its inner edge and read wider than the text half. Heights: 500px top row, 410px bottom row at lg. He also cut the dots, most of the blue, the voice wave in the lesson card (the pet sits there now, reacting to each move) and the sky underline on "human" in the title. On a pointer device the hovered card's edges turn sky. Cards: the lesson (the `compare` board after each move, a move tag, four progress segments, the pet beside the spoken line) as the anchor, about twice the others; Desmos slope with the rise highlighted; a right answer ringed; a picture cycling through negatives / algebra / ratios / percent; the pet on the hero's field saying tutor lines (so the pet appears twice; flagged to Mateo). Cards rise in with opacity and a 14px rise, 70ms apart, **never the page's blurred `Reveal`**: a board photo multiplies into its tint only if no filter-made stacking context sits between them, or its white shows as a box. The dev image optimizer hung forever on `slope.png` and `area.png` at `w=640` when the browser asked for WebP (curl without that header was instant), so the board photos here are `unoptimized` (30 to 160 KB PNGs already at 2x). Content lives in `how/moments.ts` (every tutor line points at the picture and ends on something for the student to do). New board scenes are in `lesson.ts` (`compare`, `negatives`, `slope`, `percent`, `area`, `solved`, `ratio`); `scripts/landing-shots.mjs` takes `--only a,b` (keeps the other entries in `shots.generated.ts`), `seq` photographs a scene after given call counts through the finished board's camera and crop so the frames stack (`compare-2/4/6/9.png`), and new scenes settle 7.5 s so the tutor's pen, which hides after 6 s idle, is out of the picture. Before this, `AskBoard` (`#how-it-works`, titled "Talk to it like a human tutor." since Sept 21, Mateo's own line, with the sky squiggle drawing itself under "human" (`Squiggle` in `marks.tsx`, which also holds the shared `stroke` builder the intro's strike uses); "Ask it the way you'd ask a person" was his example of AI slop copy and "Ask badly. It still gets it." was mine, which he called worse, so the section's title is his: the student's line top left, the board in the session's own window chrome with `SessionChip` and `DockBadge`, the tutor's spoken line top right (256px wide so it clears the 600px window at left 236), and a result card whose rows tick off, on a 12.5 s script), `Trio` (Sept 21, after Mateo's Aoutive pricing reference: one grid drawn in hairlines instead of three tinted tiles. A top and bottom line, a divider between the three columns, and a divider between each column's 340px picture cell and its caption cell, the grid on `.lp-railspan` so it runs rail to rail rather than sitting in the 1180 container (Mateo, Sept 21: attach the boxes to the dashed lines) and its lines end on the page's vertical rails, in `--lp-grid` (16% black: a step darker than the card hairlines, a step lighter than the dashed structure, so the three read as one system). No tints, no card shadows; the pieces sit on the page's own white, which also removed the white band above the `VoiceWave` that Mateo had called horrible in the tinted version. One seam was left: the wave's canvas paints its own background (`VOICE_BLUE.bg`, #f8f9fb, the dock's), a shade greyer than the page's #fbfbfc, so the cell showed two whites. `VoiceWave` now takes an optional `background` triple (the dock's by default) and this cell passes the page's, `PAGE_BG` in `Trio.tsx`, so the canvas rises out of the cell's own colour. The header is split like the reference, title left and lede right, bottom-aligned. Below `md` the columns stack with the same divider between them. Each cell still holds a working piece: the `VoiceWave`, the steps board writing in, the crossed-out attempt plus hint), `FeatureStack` (`#capabilities`, "Everything else a session does", a sticky deck Mateo asked for on Sept 20, its cards redrawn from his own mockup on Sept 21: four cards that pile up as you scroll. **A card is a row of the page's grid, not a card floating on it** (Mateo, Sept 21: "more wide so they connect with the big vertical lines"). It runs rail to rail on `.lp-railspan`, so the dashed rails ARE its left and right edges and it draws none of its own; its top and bottom are `.lp-deck-card`, the rails' own ink and width (`--lp-rail-ink`, `--lp-rail-w`) but solid rather than dashed. No radius and no shadow: both belong to the floating-card language this replaced. **Its fill is `--lp-bg`, the page's own white, not `--lp-surface`** (Mateo, Sept 22): a #ffffff card on the #fbfbfc page was two whites three points apart, the same defect he caught on the Trio wave cell, and here it made the card's copy half read as a panel laid over the page instead of a row of it. Sampled from the rendered pixels, the page, the card's copy half and the gap between two cards are all #fbfbfc. It also means a fully sunk card hazes to exactly the page colour, since the haze overlay is the same token, and that only the borders and the dither panel ever draw. Measured at 1440: card 1270px at x 85 to 1355, rails at 84 and 1356, so 1.0px inside each; borders 1px `rgba(18,18,21,0.3)` top and bottom, 0 on the sides. The copy column (`.lp-deck-copy`) pads 77px on the left at rail widths, which is the rail inset less its 1px less the container's 558px text half-width, so **the card's body text starts on exactly the same x as the section heading above it** (measured delta 0.0). Under 1320px there are no rails, so the card falls back to the container's text column with its own 9% hairline sides: 1116px at 1200, 350px at 390, both flush with the heading's box. The dither panel is a window INSIDE the card, not a half of it: 12px of the card's own white runs between it and every card edge it meets, top, right and bottom, with no left margin from lg because that edge is the seam with the copy column rather than an edge of the card (Mateo, Sept 22, after a round where it bled to the card's edges). The scene pieces sit 28px inside the panel, so their inset from the CARD's edge is the same 40px it was before the panel moved; if the panel's margin changes, move them by the same amount or the pictures drift.
    Each card is 520px and `sticky` 14px lower than the one before (`STEP` 14), which leaves the edge of every card underneath visible and makes the pile read as a pile. **Since Sept 22 the cards touch and the heading stays in view** (Mateo: no gaps, "always under each other, directly touching"; keep the title visible while the deck runs). There is no dwell gap any more: each card after the first is pulled up 1px (`OVERLAP`) so two borders never stack, a slot is `CARD_H - OVERLAP`, and the next card starts over a parked one the moment it arrives, which is also when the parked one starts to sink. The heading block sticks at `HEADER` (60px, the floating header's bottom) on the page colour, z-30, only as wide as the gap between the rails so the rails stay visible, and the cards park at `HEADER + heading height + under`, measured with a ResizeObserver, so a taller heading moves them rather than overlapping them. `under` is 56px (Mateo asked twice for more space), shrinking toward 32 only on a screen too short to hold the heading, the gap and the front card (39px at 1280×800, where the front card's bottom lands at 792). The deck sits the same `under` below the heading, so the first card parks at the moment the heading sticks. `useDeckTop` re-measures when the heading's height or `under` changes: a page resize did not always report it, and on a short screen the stale top made the heading leave 17px late and squeeze the gap to 22px. **The heading leaves with the pile**: a scroll-linked `y` lifts it 1:1 from `release`, the scroll at which the last card's top reaches the first card's spot (the moment the whole pile starts moving as one). Pinned until the section ended, it let the finished pile slide under it and show above and around it (Mateo caught it, Sept 22); released when the last card merely arrived, it pulled away from the still-parked cards. Measured through the exit at 1440 and 1280, sampled every 50px: the gap between the heading's bottom and the highest card never leaves 32px while the heading is on screen. The heading is Bento's split (title left, lede right in a 300px column) so the title keeps one line and the block is 134px at 1440. Measured at 1440×900 and 1280×800: heading pinned at 60, cards parked at 226/240/254 at 1440 (223 at 1280), the front card's bottom inside an 800px screen, the finished pile passing under the heading, which leaves with the section. **The pile fans, and it fans evenly.** A card does not just sink once and stop: every later card that lands takes it a step further back, `SINK` 0.022 of scale and one stop of `DIMS` each, so at the bottom the four measure 1270, 1242, 1215 and 1188px wide with the page showing through the deepest at 78%. 0.022 of a 1270px card is 14px off each side, the same as `STEP`, so a card one step back has moved the same distance on every edge and the recession reads as one straight move into the screen, which is what Mateo asked for ("the one from before kinda goes into the screen a bit"). That only works because the rails stay put: they are the window the cards recede behind, and before the cards reached them there was no fixed frame to measure the recession against. **There is no `rotateX` any more.** It was invisible (all you see of a sunk card is the 14px strip at its top, which is exactly where a tilt about the top edge moves nothing) and it made the new 1px borders shimmer on a sub-pixel angle; `perspective` went with it.
    **A card that has gone back grows its own sides** (`.lp-deck-edge`, Mateo, Sept 22: "it looks bare"). At rest it has none, because the rails are its sides; the moment it recedes it leaves them behind and its fill simply stops, which reads as unfinished. So an absolutely positioned overlay with only left and right borders, in the rails' own ink, fades in from 0 to 1 across the first step back and holds while the card goes deeper. It sits BEFORE the haze in the DOM, so the new edge recedes with the rest of the card instead of staying lit at the back of the pile, and it is `display: none` below 1320px, where the card already has its own hairline sides and never looks bare.
    **The deck ends on the last card's bottom edge** (Mateo, Sept 22: "the border should be that horizontal dashed line"). The section carries `pb-0 sm:pb-0`, the trailing spacer is gone, and the last card carries `data-last`, which zeroes its bottom border, so the NEXT section's dashed rule is drawn exactly there and becomes the card's bottom (measured delta 0.0 at 390, 1200 and 1440, complete with the crossing nodes where it meets the rails). The last card therefore never sticks: there is nothing below it to stick against, and instead of parking it carries the finished pile up and off the screen. That cost the last card its dwell, which is unavoidable, since the only thing that can give a sticky element a dwell is space after it and that space is exactly what had to reach zero.
    **The gap between cards is the deck's flex `gap`, and must never go back to being a margin.** Sticky keeps an element's MARGIN box inside its container, so a 160px bottom margin on each wrapper released every card 160px early: the whole pile was dragged off the top before the last card had parked, leaving a band of half-faded cards above it. With the gap on the flex container instead, each card is released at exactly the scroll where the last card's top edge reaches it, so the four converge on one position (measured: 104/118/132/146, then all four at 104) and the stack leaves as a single card. One `useTransform` per card carries all the stops (`at`/`scales`/`dims` built in a loop), and the last card, which has nothing behind it, still needs a non-empty range or Motion throws `mixers[i] is not a function` and the page 500s. The sink starts at `parked + GAP`, not at `parked`: before that the card is the one being read and it must not fade while nothing is over it. It hazes toward the page background (`--lp-bg`), not toward ink: a dark scrim turned the sunk cards into grey slabs. `rotateX` is 4deg about the top edge with `perspective: 1600` on the **sticky wrapper**, because perspective only applies to an element's direct children. **Three more things that will bite anyone editing this.** (1) The sink is driven by `scrollY` against the deck's measured document top, not by `useScroll` on a card: a `sticky` element's rect reports where it is parked, not where it sits in flow, so it cannot measure itself; the slot size is a constant, so each card's parked and covered scroll positions are arithmetic. (2) The deck ends with a real spacer `<div>` in flow, not a bottom margin on the last card: a margin collapses out of the container, and `sticky` is confined to its container, so without it the whole pile was clamped and slid off the top while the last card was still being read. (3) Below `lg` the device is off entirely, no sticky and no transforms, picture above words, because a 520px card that parks under a phone viewport is a card you cannot scroll past. **The card** (after Mateo's mockup): a 48px line icon in the Math Only wall's family and blue at a 1.5 stroke (`deck-icons.generated.ts`: Hugeicons Stroke Rounded teacher, pencil-ruler, idea-01 and file-edit from the Iconify API, MIT, drawn in #2f8ef7; it replaced a Fluent emoji mark on Sept 22, and 64px at 1.75 read too big and heavy), the title, the paragraph and three sky check rows on the left; on the right a **dither panel** in that card's own colour holding real pieces of the product. The panel is the hero's `DitherWave` in `swirl` mode at `waveSpeed` 0.012, a third of the hero's pace ("make it move rly slowly"), under a 15% page-colour wash, running only while the card is in view and still under reduced motion; the four palettes are in `HUES` (sky is the shared `SWIRL` preset, then violet, green and amber, the tutor's other pens paled the same way). Gradients are gone. **The card and its panel are square** (`borderRadius: 0` inline, because `.lp-card` fixes 20px from outside Tailwind's layers and `rounded-none` loses that cascade), while the pieces lying on the panel keep their corners: Mateo asked for the sharp corners to match the dashed grid, then for the inner cards to stay rounded. The four cards are: **Like a professional tutor** (the hint board, a speech bubble, and `TutorPet` at 132px, the pet another session built, switching to `state="speaking"` when the bubble shows), **It draws the idea** (the parabola with its crossings labelled, and the right triangle drawn to scale), **Explains it your way** (12 cookies in three groups, and 3/4 = 6/8 as two pies), **Reads your worksheet** (`public/landing/worksheet-page.png`, a whole printed worksheet tilted 3deg, and the board with Problem 5 copied off it). Scene scripts are module constants because `useScript` restarts when its steps array changes; they **play once and hold** (`useScript` takes `loopAt` 0 for that, added Sept 21), because a loop blanked the second board every nine seconds while the card was being read. The section is 3191px at 1440, about three and a half screens: that length is the effect, not an accident. `Showcase.tsx` is gone), `Keycaps` ("Math only / From fractions to functions.", rebuilt Sept 22 as a wall of math glyphs. `Keycaps.tsx` is now just the heading and lede; `KeycapSlider.tsx` holds three rows (six until Mateo found that too many). **Shipped look, Mateo's own tuner values (Sept 22):** no tile at all, each 28px glyph at a 1.75 stroke in #2f8ef7 framed by four 14px corner ticks in ink at 35%, on an 80px pitch cell with 15px gaps, 220px of travel, and the wall fading out over 176px at each end (capped at 20% of the wall so a phone is not mostly fade). Before that, 72px tiles, each one line icon from Hugeicons' free Stroke Rounded set (`@iconify-json/hugeicons`, MIT; 72 glyphs such as √x, Σ, f(x), sine, parabola, 3D axes, compass, cone and scatter plot, generated into `topic-icons.generated.ts` by a scratchpad one-off, not a project dependency) in #2f8ef7 at 22px on a tile of the page's sky at 10% with a 12% sky hairline and 16px corners. Mateo's asks that shaped it: no equations or text on the cards, icons at most a quarter of the card, more rows, then (after a first pass in Fluent colour emoji on 128px dotted-paper cards that he called bad) "much smaller" boxes in light blue with "a stronger light blue" icon, pointing at the Hugeicons catalogue. A single-weight line set in one hue is what makes seventy-two different symbols read as one wall; the emoji read as clip art. The glyphs are dealt round robin into six rows of twelve so no row is one topic, and each row is filled to 20 tiles (1270 + 260 travel + 74 offset needs 1604px at an 84px pitch) with eight borrowed from the row three below, so no icon repeats inside the frame and no two neighbouring rows share one; the paths live once in an SVG sprite of `<symbol>`s and each tile `<use>`s its glyph, which passes `currentColor` through. The mechanism is the Sept 19 one: rows slide in step with the page scroll through `useScroll` on the field (`["start 0.85", "end 0.15"]`), a straight `useTransform` per row with its own share of 260px (`DRIFT` 1, −0.8, 0.65, −1, 0.85, −0.7), pre-shifted by `OFFSET` values that are never a whole pitch apart so columns never line up, cut off hard at the rails (rail to rail with `overflow: hidden`, full-bleed below 1320px), no hover because nothing is clickable, and parked at mid-travel under reduced motion. The typeset-math cards with the sky underline and highlighter marks (Sept 19 to 21) and the Fluent emoji version (Sept 22 morning) are both gone. **Every visual constant is live-tunable** (`TopicsTuner.tsx`, `TopicWall` and `TOPIC_WALL_DEFAULT`): Shift+T, `?topics`, or a double-click on the wall, in development or on a build loaded with `?tune` or `?topics`; saved to `localStorage` `chalk.topics.v1`; Copy returns a `TOPIC_WALL_DEFAULT` literal to paste over the shipped one. It controls rows, tile size, gaps, corner radius, fill, edge (none, solid, dashed), corner marks (ticks, or the page's square crossing nodes), a seamless mode where tiles share hairlines like the page grid, icon size, stroke and colour, travel, direction and stagger, and a hard cut or fade at the edges, with presets toward the page's language: Tiles, Square, Corners, Nodes, Grid, Grid + nodes. Two mechanics to keep: the stroke width is set by CSS on the sprite (`[&_*]:[stroke-width:var(--lp-topic-stroke)]`), which beats the glyphs' own attribute and reaches every `<use>` copy; and the row transforms read their ranges from a ref nudged by a motion-value counter, because array-form `useTransform` ranges are fixed at first render and would ignore the tuner until reload. In the seamless grid each row draws nodes only on its top line (the last row on both), since rows sit at different offsets and a row's bottom nodes never meet the next row's top ones), `ForParents` (`#parents`: four grey wells each holding a small settings card of true facts: help order, transcript, what is kept, subjects), `Reviews` (`#stories`, Sept 20, rebuilt Sept 21: "Hear from happy students", a wall that keeps moving — three columns of quote cards drifting vertically at 20, −16 and 24 px a second, the middle one against the others, masked out to nothing at the top and bottom so it runs off the page rather than stopping at an edge. Each card is five gold stars (`#f0b429`, the page's only yellow), a short quote with the board's sky highlighter (`Mark`) on the words that matter, and who said it. Hovering one column stops that column alone so it can be read. The loop is the cards rendered twice with the offset wrapped at one copy plus the gap, driven by `useAnimationFrame` on a motion value rather than a CSS keyframe, so it needs nothing in `globals.css` and hover can pause it. How many columns there are is read with `matchMedia` and the quotes are dealt between them round robin, because hiding columns with CSS would drop their quotes on a phone. Reduced motion gets every card standing still in a plain grid, not a window that hides most of them. **Shipped look since Sept 22, Mateo's own tuner values:** no card fill, border or radius, just 18px corner ticks in ink at 35% around each quote (the topics wall's corner language, so the two sections match), 21px padding, and the key words in bold (weight 600) instead of the sky highlighter; the old white rounded cards are the tuner's "Cards" preset. **Live-tunable** (`ReviewsTuner.tsx`, `ReviewsLook` / `REVIEWS_DEFAULT`): Shift+R, `?reviews`, or a double-click on the wall, saved to `localStorage` `chalk.reviews.v1`, Copy returns a `REVIEWS_DEFAULT` literal. It switches the layout between drifting columns, sideways rows (a marquee of fixed-width cards) and a still grid, and exposes column and row counts, height, card width, gap, container or rail-to-rail width, speed, direction, hover pause, edge fade, the card's fill, radius, padding, shadow, edge, corner ticks or nodes, the body or display face, text size, alignment, quote marks (inline or a big sky mark), how the key words are marked (marker, underline, blue, bold, none), stars and avatars. Presets: Current, Corners (the topics wall's shipped look), Grid, Tinted, Lifted, Minimal, Big quotes, Marquee, Still grid. The highlighter reads the look through a React context because it is written inside each quote. Both section tuners share `useTuner.ts` (the hydration-safe saved state, Shift+key and `?param` opening, `asLiteral` for Copy, `rgba`, and `cornerTicks`). Before it was a static three-column grid with one featured quote on a `Band`; initials in a sky circle, no photos, no marquee, no dark band. EVERY QUOTE IS A PLACEHOLDER, see the note at the top of the file: none may ship, fake testimonials are illegal, and since Sept 21 each card also shows five stars, which is a rating claim stacked on a fabricated quote. The warning comment at the top of `Reviews.tsx` stays until the quotes are real and permissioned), `Pricing` (`#pricing`, Sept 20, rebuilt on the grid Sept 22 after Mateo's Aoutive reference: Free, Plus $19 and Family $29 as **one table that is a row of the grid**, on `.lp-railspan`. The frame's top and bottom are `--lp-rail-ink`, solid, and it draws no sides from 1320px, where the rails are its sides (9% hairline sides below that); the two column dividers are a step lighter in `--lp-grid`, so the frame reads as the grid and the insides as its subdivision. **No lines inside a column**: the first version ruled a band under each of plan, price, button and list, three extra lines across the table, and Mateo cut them ("way too many horizontal lines"), which also took the table from 604px to 476px tall at 1440. The four bands are still shared `grid-rows-subgrid` rows, so the prices, the buttons and the lists start at one height across all three columns whatever each plan's copy runs to. The page's node sits at all eight crossings (the rail corners and where the two column dividers meet the frame), from 1320px only, like every other node. From 1320px each band pads `calc(var(--lp-rail-inset) - var(--lp-rail-w) - 558px)`, the deck card's 77px, so the first plan's name starts on the heading's exact x (measured delta 0.0), the reference's own logic of the heading and the first column sharing one margin from the frame; that padding carries a `!` because Tailwind orders an arbitrary `min-[1320px]` variant before the named `lg` and `lg:px-10` would win on source order. The header is Trio's split, title left and lede right, and the right column holds the monthly and yearly toggle, the one control the table answers to. Plus stands on a `Band` behind its whole column. The buttons are **the hero's pair** (Mateo, Sept 22): Plus has the pressed `.lp-btn` with its arrow, Free and Family `.lp-btn lp-btn-quiet`, exactly as "Try a session free" and "See how it works" sit in the hero, so the table still has one primary. A glossy black button with hairline outlines was tried first and replaced. No radius and no shadow on the table; beUI's `Tabs` (`components/motion/tabs.tsx`, segment variant, its radius edited to the page's 10px; the CLI stops to ask about `lib/utils.ts`, see Gotchas) switch monthly and yearly, and the prices roll with `@kitlangton/rolling-number` (MIT; its stylesheet is imported in the component). The tiers, limits and prices are made up: there is no billing, no family accounts and no daily limit), `Faq` (`#faq`, rebuilt Sept 22 from Mateo's idea: **the tutor's pet answers the questions**. Labelled "FAQ"; the heading stays on the left (sticky) and the questions on the right, as before. The rows are hairline rows with a small 16px plus, two 1.5px bars and no circle, that turns 45deg into a sky-deep cross over 450ms. Tailwind's `rotate-*` sets the CSS `rotate` property, so the transition must name `rotate`; naming `transform` is why the first version jumped instead of turning. One row is open at a time, Escape closes it. Opening a row drops it down and an 84px pet pops in underneath (`arrive`); 280ms later its speech bubble grows out of its tail beside it with the answer, the pet talking for about as long as the words take and then resting. Closing the row takes the pet and the bubble away with it. The row's height follows its content on one spring (`Grow`: the content is measured, so it opens from nothing, grows when a thought becomes an answer, and closes back), and every ease uses one curve, so the section moves as one thing. **The bubbles are the FAQ's own, not the shared `PetBubble`** (Sept 22, after Mateo called the first ones sloppy): that one animates its size while its words arrive, so on a long answer the words sat outside the box mid-animation. These lay out at their final size first and only fade each word up out of a blur where it already sits (measured: 0px of any word outside its bubble at every sample through the reveal), a hairline on white with a small tail and no shadow. **Thinking** is the same bubble holding the word "Thinking" with a sky light sweeping across it (a clipped gradient, 1.5s a pass) and two small circles trailing back to the pet, pulsing in turn; it replaced `PetBubble`'s cloud and its three plain dots, which Mateo found ugly. The thought and the answer share one grid cell, so one leaves while the other arrives in the same place. **The last row is an infinite FAQ** (the idea @heyimgustavo's post made popular in September 2026): a bare "Ask anything else…" input whose send button is just an arrow, invisible until there is something to send and fading away (drifting 8px on) the moment it is sent. The question takes ONE asked row at the end of the list, replacing the last thing asked rather than piling up (the new question blurs in over the old one), and opens with the pet thinking (at least 1.1s, so it reads as thinking) while `POST /api/faq` asks the model, then saying the answer, or looking puzzled and saying so on an error. Cut on Mateo's review of the first version: a sticky pet column beside the list that answered without unfolding the rows, the "Answered live by the tutor, not reviewed by a person" line, three suggestion chips, and a round black send button. `lib/faq.ts` (pure, tested) is the one source: six curated questions, rewritten Sept 22 as the worries a parent has before trying a voice AI tutor rather than features the page already shows (how is this different from ChatGPT, does it just give my kid the answer, what if it gets something wrong, does my kid have to talk out loud, can I see what they worked on, what does it cost; the subjects-and-grades and worksheet questions went because the "Math only" section and the worksheet card already answer them), with answers in the pet's first person, the facts a live answer may use, the system prompt (one to three sentences under 50 words, only the facts, never a price since plans aren't final, math and off-topic questions redirected to a session, no model names, ignore injected instructions) and `clampAnswer` (markdown stripped, cut to whole sentences under 320 characters). A live answer only knows what the facts say: "can two kids share one account" got "not sure" until the one-student-per-account fact was added, so a new "not sure" in testing usually means a missing fact. `app/api/faq/route.ts` is public (listed in `proxy.ts`) and calls `openai/gpt-6-luna` through the AI Gateway at low reasoning effort (`FAQ_MODEL` overrides), with a 15 s timeout and in-memory limits per server instance (8 a minute and 60 a day per IP, 1,500 an hour for everyone), which stop a script from running up a bill but are not a real quota. Measured Sept 22: 1.1 to 2.6 s an answer, about 900 tokens in and 35 out, about $0.0001 a question; the cheapest gateway models would save a fraction of a cent per thousand and answer worse. Answers vary a little run to run. Found while building it: `PetBubble` measured its cloud only on mount, so a thought bubble that started closed drew only its dots; it now measures whenever it opens), `Ending` (Sept 22, **replaces both `Closing` and `Footer`**, both files deleted). The page used to end twice: `Closing` ("Stuck on tonight's homework? Start talking." on a full-bleed `Band`) and then `Footer` under it on the deep-blue `FooterWave`, two shaders and two sign-offs. `components/landing/Ending.tsx` is one ending, the option Mateo picked from four on Sept 22 ("Horizon"; Dusk, Grid and a glass Bookend bar lost, and the temporary picker is gone): the closing line, the pitch, the hero's `.lp-btn` and the links on the page, then the hero's light field (`HERO_FIELD` under a 45% wash) rising from the bottom and masked out as it goes up, behind the wordmark in ink (white vanishes on a light field). The wordmark was 192px and Mateo asked for it smaller: `clamp(3.25rem, 8.5vw, 7.5rem)`, 120px at 1440 and 52px at 390, in a field box of `clamp(200px, 22vw, 320px)`. Under it, "Built by students, for students." (Mateo's line, replacing "Built by a student, for his sister.") and the copyright. The headline is one sentence per line (width-wrapped it stranded "talking."). **It is the page's `<footer>` landmark, outside `<main>`, so it carries its own `.lp-rails`** with `--lp-rail-lead: 0px`: its rails start at its top with no fade-in, exactly where main's rails stop (measured: main's rails end at the footer's top, both at x 84 at 1440), and the join falls under the node where the section's rule crosses them, so the dash phase change never shows. The header and footer link to Pricing. Left out on purpose because there is no proof to show yet: logo walls, stat bands, security badges. Gone: `Statement` (the one-paragraph quote about Mateo's sister, removed at his request on Sept 22; its React Bits `ScrollReveal` is now unused), `HowItWorks`, `Bento`, `Parents`, `Founder`, `TopicsMarquee` (it listed chemistry and essays), and the vendored `CardSwap`, `SpotlightCard`, `bento-grid`. **Sept 20, tried and reverted:** a rebuild of the whole page on library blocks after Aristotle's argument (a merged how-it-works stage with a three-step rail and two claim rows, beUI segmented tabs over the session window, the topics slider on a blue band with one-pen cards, an ink testimonials band on Sona UI's marquee). Mateo: "this is worse than before"; he asked for yesterday's page back plus the two new sections, and the reviews redone "not the black theme". Do not bring back a dark band (rejected twice now, Sept 13 and Sept 20) or the one-pen keycaps. Verified at 1440 and 390 with screenshots and a reduced-motion pass; the first Sept 16 round had the new classes missing because Turbopack served a stale `globals.css` (see Gotchas).
- **Nothing on the page imitates the app any more; it uses the app.** `SessionDemo.tsx` is the live session screen inside the hero frame: a static replica of the collapsed rail, then the real `SessionChip`, `VoiceDock` (transcript sheet, composer, mic, Files popover all work), `CaptionBar`, and the real tldraw `Whiteboard` replaying `components/landing/lesson.ts` through `dispatchWhiteboardTool`, on a 42 s loop. The board mounts only once the hero is near the viewport and the screen is ≥768px (`autoFocus={false}` so tldraw never steals focus and scrolls the page; the wrapper is `pointer-events: none` so wheel scrolls the page). Phones get `public/landing/session-board.png` instead. The chrome (rail, chips, dock, captions) is scaled with CSS `zoom` to the proportion it has on a ~1480px screen (`scale` in `SessionDemo`), so it reads like a screenshot of a whole screen; the board keeps its own zoom, and the dock gets `frameHeight / scale`. The rail is a static replica of the collapsed `AppSidebar` built from the app's own `.sb-btn` / `.sb-item` / `.sb-toggle` classes, so style changes carry over; a change to its layout or icons still has to be copied by hand. Typing to the tutor adds the line to the transcript and the demo says it only replays a lesson.
- **The tiles show photographs of the real board.** `scripts/landing-shots.mjs` (needs the dev server and Chrome) replays each scene in `lesson.ts` on `/dev/landing?scene=…` (dev only, behind QA login), trims to the drawn pixels at 2x, writes `public/landing/*.png` and `components/landing/shots.generated.ts` (sizes for `next/image`). Rerun it whenever a board renderer changes. `Fragments.tsx` has `BoardShot` (photo written in from the top), `Transcript` (the app's `TranscriptList` in the sheet's chrome) and `DockBadge` comes from `VoiceDock.tsx`; `FilesPanel` (the Files popover body) is exported from `FilesPopover.tsx` for the worksheet tile. Scenes added Sept 21 for the feature cards: `graph` (a parabola with its crossings and vertex labelled), `figure` (a right triangle, legs 6 and 8), `icons` (12 cookies in three groups), `fraction` (3/4 and 6/8 as two pies) and a rewritten `worksheet` (Problem 5, y = 2x + 1 graphed). A Desmos scene needs a longer `settle` in `scripts/landing-shots.mjs` (5000ms; the others take 1800) or the shot catches an empty grid. `public/landing/worksheet-page.png` is **not** a photograph of a real worksheet: it is an 800x1001 reproduction of the one Mateo attached, rendered from HTML in the scratchpad, because a chat attachment is not reachable from the repo. Drop his original over it whenever it is to hand. `proxy.ts` lists `/landing` as a static path so the image optimizer can fetch the photos.
- Reduced motion: every landing component reads the preference through `useReduce()` in `useScript.ts`, which returns null until hydration (Motion's own hook knows the answer on the client's first render and the server does not, which was a hydration error under reduced motion). `useScript` treats reduced motion as "final frame".
- `DitherWave` reads every prop on each frame through refs, so the tuner changes it live without rebuilding the program; its clock advances by speed per frame, so a speed change never jumps the field, and `lightness` shifts the tone (0 keeps the calibrated look). It has two patterns: `bands` (default, no longer used by the hero) and `swirl` (fbm warped by itself twice, used on sign-in and behind the landing hero through the shared preset `components/landing/swirl.ts`), plus an optional `deepColor` third tone. The swirl samples noise five times per pixel with 4 octaves and is tuned to be mostly colour with a little background (the field's value runs through four breakpoints on a deepest blue, light blue, background ramp, with slow circular drifts that loop seamlessly every four minutes (`circ()` paths in the shader, clock wrapped at 84 units by `SWIRL_LOOP`), so large soft shapes move on instead of parking in a corner and the look never drifts; straight-line drifts grew without bound, and after tens of minutes the noise lost GPU precision and the panel washed out; the sign-in panel draws seven shades so broad areas show soft gradations; the breakpoints were fitted over one full loop to the seven-shade mix of the opening minutes, so every moment matches how it starts (mean lightness about 139/255, about 3% white), with the top one high so only the brightest peaks turn white; contour-band ribbons were tried and rejected as too busy and too white, and shrinking the shapes removed little patchiness while adding detail; on sign-in the deepest tone is the voice deep blue pulled 40% toward the voice light blue; the wordmark on the panel is white, and a `calmSpot` fade anchored at the bottom-left corner (420 by 230px, cap 0.55) keeps that corner blue and never white; a small oval centred on the wordmark read as a floating pill); it pauses off screen like the bands.
- Own tokens under `.lp` in `app/globals.css` (off-white / off-black / light gray + sky accent). Buttons are `.lp-btn`: white face, 2px ink border, solid offset shadow that the face slides into on hover. Radius rule: 10px interactive, 20px surfaces, `.lp-frame` = double border.
- Fonts via `next/font` in `components/landing/fonts.ts`: Schibsted Grotesk (display), Hanken Grotesk (body), Shantell Sans (board handwriting, the same face tldraw uses), Sora (the wordmark). **Brand rule:** the word "chalk" beside the mark is always Sora at weight 650 with -0.04em tracking, via `.lp-brand` (`Wordmark`, the sidebar header, the sign-in panel). The mark is always the plain sky scribble `ChalkMark`, never the old ink tile: `Wordmark` (landing header and footer) renders it at 1.45x the text size, the same proportion as the sidebar.
- Registries in `components.json`: shadcn (base-nova style, Base UI), `@magicui`, `@animate-ui`, `@react-bits`, plus `@reactbits-starter` / `@reactbits-pro` which need `REACTBITS_LICENSE_KEY` in `.env.local` before `npx shadcn add @reactbits-pro/faq-2` or `@reactbits-starter/dither-wave-tw` will install. 21st.dev now requires an account for its registry.
- Vendored registry copies (`components/*.tsx`, `components/ui`, `components/animate-ui`) are ours to edit; a few carry small typing patches.
- The landing's "Try a session free" buttons still open `/signin` in sign-in mode; `/signin?mode=signup` opens account creation.

## Commands
- `npx tsx scripts/tutor-eval.ts` — teaching eval (see "How the tutor teaches"); `npx tsx scripts/board-eval.ts` — board-use eval; `npx tsx scripts/live-model-probe.ts [model …]` — open a real Live session per model with our prompt, tools and audio config and compare what comes back (see "Voice provider switch").
- `npm run dev` — dev server. `npm test` — `tsx --test lib/*.test.ts` (every pure module in `lib/` has one). `npm run vendor:stretch` — re-vendor and patch the voice speed engine after upgrading `signalsmith-stretch`. `npm run vendor:pdfjs` — copy pdf.js's worker after upgrading `pdfjs-dist` (then bump `PDFJS_VERSION`). `npx tsc --noEmit`. `npm run lint` (five pre-existing errors in vendored registry components: `Magnet`, animate-ui's tabs and highlight, `dot-pattern`; `public/vendor` and `.playwright-mcp` are not linted).
- QA without a microphone: open `/api/dev/qa-login` (dev only) → lands on `/session?debug=1`, a text-only session with the QA panel. Add `&mic=1` for a real mic plus the panel. In dev, `window.__liveTutor` exposes `debugStats()` and `debugSend(event)`.

## Environment (`.env.local`)
`OPENAI_API_KEY` (required), `DATABASE_URL`, `AUTH_SECRET`, `NEXT_PUBLIC_TLDRAW_LICENSE_KEY`. Optional: `OPENAI_TUTOR_BACKEND_MODEL` (default `gpt-5.6-terra`; `gpt-5.6-luna` is ~10x cheaper), `OPENAI_TUTOR_REASONING_EFFORT` (default `low`). Vercel needs the same variables in BOTH the Preview and Production environments (the Preview environment was missing `DATABASE_URL`, `AUTH_SECRET`, and the tldraw key, which is why every branch deploy failed). The DB client no longer throws at build time when the variable is missing; the first query fails instead. Gemini: `GEMINI_API_KEY` (server only). `/api/live-token` falls back to `NEXT_PUBLIC_GEMINI_API_KEY`, which Vercel Production still carries from the browser-side era; once `GEMINI_API_KEY` is set on Vercel, remove the public one and rotate the key. `NEXT_PUBLIC_TUTOR_PROVIDER` picks the default provider (see "Voice provider switch"). `NEXT_PUBLIC_DESMOS_API_KEY` is the Desmos partnership key for graphs (see "Graphs: Desmos"); without it production draws vector graphs and does not offer `draw_desmos` or `draw_data_plot`. `NEXT_PUBLIC_DESMOS_TOOLS` ("all", "none", or "add_number_line,draw_bar_chart,draw_figure") picks the pictures Desmos draws; unset, every build draws all of them on Desmos (Mateo's call, Sept 17 2026). `FAQ_MODEL` overrides the landing FAQ's live-answer model (default `openai/gpt-6-luna` through `AI_GATEWAY_API_KEY`, else `gpt-6-luna` on `OPENAI_API_KEY`); with neither key the ask row answers with an apology.

## Costs
gpt-live-1 bills $0.05 per minute of session, plus backend tokens. Expect roughly $1.60–2.10 per 30-minute session with terra.

## Gotchas
- Branch order: `gpt-live` > `v2` > `main`. Never base work on `main`.
- Background agents must not run `git checkout` / `switch` / `stash`; one did during a read-only audit and moved the working tree to `main`.
- `.playwright-mcp/` is gitignored; Playwright MCP writes screenshots and snapshots there.
- Turbopack sometimes keeps serving a stale `globals.css` after an edit (the class you just added is missing from the served stylesheet). Fix: stop the dev server, `rm -rf .next`, start it again.
- Never give every element a transition, not even a tiny one. `transition-property` defaults to `all`, so the old reduced-motion reset in `globals.css` (`transition-duration: 0.01ms !important` on `*`) turned every style change into a transition, and tldraw, which measures text by restyling one hidden element and reading its size at once, got the previous size back. Under reduced motion every board note measured one line tall and the board overlapped; normal motion hid it because the typing animation measures the text again later (found Sept 16 2026; the reset now sets `0s`).
- tldraw colours are registered globally by whichever editor mounts, and `registerColorsFromThemes` also removes colours missing from its themes. Every board must mount with `BOARD_THEMES` (today `TldrawCore` is the only mount), or shapes in `sky`, `sky-deep` or `pencil` fail validation.
- Every tool call blocks Gemini 3.1 Flash Live until its response arrives, so a tool handler must answer fast (the client caps it at 3 s). A picture the tutor should see with the answer (`look_at_board`, `look_at_worksheet`) is sent before the response, never after, and at most about once a second.
- The Playwright MCP browser window is shared: other things can click in it mid-check (on Sept 16 a demo nav link was clicked during a run and the numbers came from the wrong demo). For board checks whose numbers matter, drive a private headless Chrome with `puppeteer-core` (already a dependency, as `scripts/landing-shots.mjs` does) and confirm each result matches the calls you sent.
- Registry components that read Motion's `useReducedMotion` and render a different tree for it (Sona UI's marquee did) are a hydration error for anyone with reduced motion on; swap in `useReduce` from `lib/reduced-motion.ts`, which is null until hydration. `npx shadcn add @beui/<name>` writes the component and then stops to ask about overwriting `lib/utils.ts` (ours re-exports the `cn` package, which resolves conflicts like tailwind-merge); answer no, the component is already in place.
